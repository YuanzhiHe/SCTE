# SCTE-R 第一阶段执行报告 · 公开数据集预训练

> 对应交接文档：`STAGE1_公开预训练_交接.md`。本报告记录实际执行过程、发现并修复的问题、
> 训练/评估结果，以及交给第二阶段（私有数据微调）的产物与建议。
> 执行环境时间：2026-08-16。

---

## 0. 一页结论

- **本阶段目标达成**：在公开薄层（RPLHR-CT，100 例训练）上跑通 SCTE-R 单次前向管线，产出
  `public.pt`。独立测试集（RPLHR 官方 val 50 例，训练与选模全程未用）上
  **LAA-950 MAE 0.67 → 0.39 pp、CCC 0.070 → 0.875、Perc15 MAE 18.28 → 2.84 HU、PSNR 28.40 dB**
  （高于厚层基线 28.36 dB）。交接文档 §4 的三条判据：1 达成、3 达成、2（CCC→0.9）0.875 边缘达成。
- **额外产出** `public_8mm.pt`（8mm/1mm，为下一阶段安阳 8mm 铺路）：LAA MAE 0.67 → 0.37 pp。
- **必须修的三个代码问题**：合成厚层噪声单位错了 1000 倍（观测被噪声淹没）、轨迹护栏梯度量级
  失衡导致发散（需 `--clip 1.0`）、`train.py`/`evaluate.py` 从未读取 `configs/default.yaml`。详见 §3。
- **`configs/default.yaml` 的 loss 权重是对的**：`w_traj=0.5` 调小到 0.05 反而随训练恶化；
  置 0（纯图像保真）则 LAA 几乎修不动（CCC 0.368）——护栏就是密度学修正的来源。详见 §4.4。
- **还不能直接上私有数据**：真实厚层输入下 LAA-950 只修回 15%（CCC 0.470），Perc15 则迁移良好
  （−83%）；且 `--data real` 的数据通路（整卷/无 patch、无 z 对齐、无 `pair_audit.csv`）尚未打通。详见 §4.7、§5.2。
- **真实配对微调排练已做**（§6，用 RPLHR 自带 100 对真实 5mm/1mm）：真实输入上 LAA MAE
  0.56 → **0.41 pp**、CCC 0.472 → **0.840**，产出 `public_realft.pt`。但逐张量比对发现
  **微调只改动了输出偏置这一个标量（−12.2 HU）**，只替换该标量即可复现全部收益；且**从零训练
  结果完全相同**——这一步公开预训练没有体现出增益，且 Perc15 由 3.58 退到 4.23 HU。
  真实层厚拟合为 6.25 mm（在训练分布内），高斯 SSP 无法解释的残差占厚层 std 的 12.8%。

---

## 1. 环境

| 项 | 值 |
|---|---|
| GPU | NVIDIA GeForce RTX 5080 16GB（sm_120） |
| 驱动 / CUDA | 580.159.03 / CUDA 13.0 |
| Python | 3.11（conda 环境 `scte`，路径 `/home/prinlab/miniconda3/envs/scte`） |
| PyTorch | 2.11.0+cu128（sm_120 需要 cu128 轮子，`torch.cuda.is_available()=True`） |
| 其它 | numpy 2.4.6 / scipy 1.17.1 / SimpleITK 2.5.6 / pydicom 3.0.2 / PyYAML 6.0.3 |

```bash
conda create -y -p /home/prinlab/miniconda3/envs/scte python=3.11
/home/prinlab/miniconda3/envs/scte/bin/pip install torch --index-url https://download.pytorch.org/whl/cu128
/home/prinlab/miniconda3/envs/scte/bin/pip install numpy scipy PyYAML tqdm SimpleITK pydicom scikit-image
/home/prinlab/miniconda3/envs/scte/bin/python scripts/smoke_test.py     # -> [smoke] PASS
```

`smoke_test.py` 在代码改动前后均通过（M1–M5 前向 + 4 项 loss + 反传）。

---

## 2. 数据

### 2.1 用了哪个公开集

交接文档首选 **RPLHR-CT**。原 GitHub 仅提供 tiny 子集（百度网盘），但作者已于 2025-10 把
**完整 250 例数据集发布到 Zenodo**（CC-BY-4.0）：

- 记录：`https://zenodo.org/records/17239183`（train.zip 14.8 GB / val.zip 6.9 GB / test.zip 12.5 GB）
- 每个 zip 内含 `1mm/`（薄层）与 `5mm/`（真实厚层）各若干 `CTxxxxxxxx.nii.gz`

本阶段**只用 1mm 薄层**（厚层由 SSP 算子在线合成）；RPLHR 自带的真实 5mm 按交接文档留给下一阶段做
sim-vs-real 对照，已下载但未参与训练。

> Zenodo 单连接只有 ~0.15 MB/s，用 16 路分块并行下载（`Range` 请求）稳定到 ~10 MB/s。
> 脚本：`scripts/pget.py`（本次新增的下载辅助工具）。

### 2.2 关键发现：RPLHR 的体数据不是 HU

RPLHR 发布的 `.nii.gz` 是**归一化到 [0,1] 的 float32**，不是 HU。逐例核验（直方图峰值）确认线性映射为

```
HU = value * 3072 - 1024
```

核验证据（10 例抽样一致）：体外空气峰 → −1024 HU（数据在 −1024 处截断）、肺实质峰 → −919…−947 HU、
纵隔软组织峰 → +34…+54 HU。

如果不做这步反归一化，`prep_public.py` 的 `_is_ct_like()`（要求 `min <= -500`）会把 100 例**全部跳过**。
为此给 `prep_public.py` 增加了 `--rescale_hu SLOPE INTERCEPT`。

### 2.3 预处理

```bash
python scripts/prep_public.py --in DATA/rplhr/train/1mm --out DATA/public_thin_all \
       --lung_crop --rescale_hu 3072 -1024
```

- SimpleITK 读取 → 反归一化到 HU → 裁剪到 [−1000, 200] → **肺部包围盒裁剪** → 存 `float32 (D,H,W)` 的 `.npy`
- 100 例全部成功、0 跳过；典型形状 `(276,512,512) → (255,269,399)`；共 14 GB
- 官方 val 的 50 例同样处理，作为**完全独立的测试集**

**为什么加 `--lung_crop`（可选开关，默认关闭）**：训练是在原体数据上随机取 `(40,64,64)` 的 patch，
512×512 全胸体数据里大部分 patch 落在体外空气/胸壁上；而 `metrics._lung_mask` 的窗口是 HU∈[−1000,−500]，
体外空气（截断到 −1000）会被当成"肺"且全部 < −950，LAA-950 恒等于 100%，指标完全没有意义。
裁到肺包围盒后，patch 才落在实质里。

### 2.4 划分

| 集合 | 目录 | 例数 | 用途 |
|---|---|---|---|
| train | `DATA/public_thin` | 85 | 训练（RPLHR train 100 例中随机 85，seed=42） |
| val | `DATA/public_thin_val` | 15 | 选模型（best checkpoint） |
| test | `DATA/public_thin_test` | 50 | 最终汇报（RPLHR 官方 val 划分，训练全程未用） |

---

## 3. 代码问题与修复

### 3.1 阻断性问题（不修则本阶段不可能达标）

**(1) `datasets.py` 噪声单位错误 —— 观测厚层被噪声淹没**

体数据以 kHU 承载（`HU_SCALE=1000`），但噪声按 HU 数值直接相加：

```python
xn = x + torch.randn_like(x) * self.thin_noise      # 15.0 → 15 kHU = 15000 HU
y  = op(xn[None])[0] + torch.randn(1) * 0           # 这行结果立刻被下一行覆盖（死代码）
y  = op(x[None])[0] + torch.randn_like(...) * self.thick_noise   # 7.0 → 7000 HU
```

实测：薄层信号 std = 46 HU，而合成厚层 std = **6981 HU**，信噪比 ≈ 0。
按 `simulate_thick_from_thin()` 的原意修正为 HU→kHU 换算，并让薄层噪声真正进入前向链：
修复后厚层 std = 17 HU（薄层 46 HU），符合厚层在 z 向平均掉高频纹理的物理。

**(2) 轨迹护栏 loss 量纲失衡导致训练发散**

`L_traj` 的 LAA 项以"百分点"为单位，且 `laa_soft` 的 sigmoid 温度 τ=0.005 kHU(=5 HU) 很陡，
其对单体素的梯度约为 `100/(N·4τ)`，比体素 L1 的 `1/N` 大约三个数量级。在 `lr=1e-3` 下训练会周期性被打飞
（同一 epoch 内 |LAA err| 在 0.3 pp 与 19 pp 之间跳）。
**加梯度裁剪 `--clip 1.0` 后稳定收敛**，这是本阶段唯一必须的超参改动（loss 权重仍用 `configs/default.yaml` 原值）。

**(3) 训练/评估完全没有读 `configs/default.yaml`**

交接文档写"超参见 `configs/default.yaml`，命令行同名 flag 覆盖"，但 `train.py`/`evaluate.py` 里
patch、`base_ch`、`n_blocks`、`dc_steps`、loss 权重、ladder 全是硬编码默认值，config 从未被读取。
已补齐：两者都读 config，并新增同名覆盖 flag（`--patch --w_dc --w_traj --w_nps --slice_fwhm ...`）。

### 3.2 让指标有意义的改动

**(4) patch 采样必须落在肺实质里**（`datasets.py`）
每例缓存一张粗网格（stride 8）"候选 patch 含肺比例"图（`_lungcache/`，随 patch 尺寸分别缓存）：
训练时在含肺 ≥ 25% 的候选里随机取并做子格抖动；评估时取含肺比例最高的位置（确定性）。
效果：评估 patch 的肺窗占比从 0.02–0.95 起伏变成稳定 0.96–0.98。

**(5) 训练时每个 epoch 重新取 patch**（原实现 `default_rng(i+12345)` 使每例的 patch 终身固定，
85 例就只有 85 个训练样本）。新增 `random_crop` 开关：train 打开，评估保持确定性可复现。

**(6) I/O**：`np.load(...).astype(float32)` 会把整卷（100–300 MB）读进内存再裁 patch。
改成 `mmap_mode="r"` 先裁后转，单样本 I/O 降到 ~10 MB。

### 3.3 新增的辅助能力（不改变既有行为）

- `prep_public.py`：`--rescale_hu`、`--lung_crop`、`--limit`
- `train.py`：`--val_root`（每 epoch 在留出集上算 LAA MAE/CCC/PSNR 并存 `*_best.pt`）、`--clip`、`--ema`、`--workers`、`--seed`、config 读取
- `evaluate.py`：config 读取、`--patch`、`--csv`（逐例结果）、同时打印 thick 基线的 PSNR/Perc15/CCC 便于对照
- `scripts/pget.py`：Zenodo 分块并行下载器（断点续传）
- `scripts/sim_vs_real.py`：同一权重在"合成厚层 vs 真实厚层"上的密度学对照（见 4.7）

---

## 4. 训练与结果

### 4.1 命令

```bash
# (a) 交接文档原样命令（作为对照记录）
python -m scte_r.train --data simulated --root DATA/public_thin \
       --epochs 50 --bs 2 --lr 1e-3 --ckpt public.pt

# (b) 实际交付权重（= 文档超参 + 梯度裁剪 + 权重 EMA + 留出集监控）
python -m scte_r.train --data simulated --root DATA/public_thin \
       --val_root DATA/public_thin_val --epochs 200 --bs 2 --lr 1e-3 \
       --clip 1.0 --ema 0.999 --ckpt public.pt

# 评估（默认 config patch 40x64x64）
python -m scte_r.evaluate --data simulated --root DATA/public_thin_test \
       --ckpt public.pt --csv results/eval_test.csv
```

loss 权重、patch、模型宽度全部沿用 `configs/default.yaml` 原值（`w_dc 1.0 / w_traj 0.5 / w_nps 0.1`）。
**唯一必需的改动是 `--clip 1.0`**；`--ema 0.999` 不是必需，但它把"验证指标在 epoch 之间大幅抖动"这个
问题消掉了（见 4.4）。单次训练在 RTX 5080 上约 20–40 分钟（85 例 × 42 step/epoch × 200 epoch）。

### 4.2 主结果（独立测试集：RPLHR 官方 val 的 50 例，训练与选模全程未用）

层厚比 5mm/1mm，评估 patch = config 的 `(40,64,64)`。

| 模型 | LAA-950 MAE ↓ | LAA-950 偏倚 | LAA-950 CCC ↑ | Perc15 MAE ↓ | PSNR | SSIM |
|---|---|---|---|---|---|---|
| **厚层输入**（三线性上采样） | 0.67 pp | −0.67 pp | 0.070 | 18.28 HU | 28.36 dB | — |
| 文档原配方（50 ep，无 clip） | 0.44 pp | −0.40 pp | 0.745 | 5.27 HU | 26.71 dB | 0.667 |
| + clip（M1，150 ep） | 0.48 pp | — | 0.751 | 3.51 HU | 28.47 dB | 0.747 |
| **+ clip + EMA（交付：`public.pt`）** | **0.39 pp** | **−0.39 pp** | **0.875** | **2.84 HU** | **28.40 dB** | 0.747 |
| 消融：`w_traj=0`（纯图像保真 ≈ DLS） | 0.61 pp | — | 0.368 | 10.03 HU | 28.58 dB | 0.747 |

同一权重在 15 例内部验证集上：LAA-950 MAE 0.64 → **0.32 pp**，CCC 0.064 → **0.891**，
Perc15 MAE 16.71 → **2.64 HU**，PSNR 28.54 → 28.58 dB。

### 4.3 验收判据核对（交接文档 §4）

| 判据 | 结果 | 结论 |
|---|---|---|
| 1. `LAA-950 MAE(SCTE-R)` 明显小于 `LAA-950 MAE(thick)` | 测试集 0.39 pp vs 0.67 pp（−42%）；验证集 0.32 vs 0.64 pp（−50%，偏倚 −0.11 vs −0.64 pp） | **达成** |
| 2. `LAA-950 CCC(recon vs ref)` 趋近 1 | 测试集 0.875、验证集 0.891（厚层基线 0.070） | **基本达成**（0.9 门槛边缘，见 4.5） |
| 3. PSNR 为正且随训练上升、Perc15 MAE 下降 | PSNR 19.1 → 28.6 dB（且高于厚层 28.36）；Perc15 MAE 18.28 → 2.84 HU（−84%） | **达成** |

> 合成配对上的数字只证明管线与优化正确，不构成临床声明——临床结论按交接文档留给下一阶段的私有真实配对。

### 4.4 三个值得记录的实验结论

**(1) 轨迹护栏（M4）是密度学修正的来源，而不是锦上添花。**
`w_traj=0` 的消融：PSNR 反而最高（28.58 dB），但 LAA-950 MAE 只有 0.61 pp（几乎等于厚层的 0.67）、
CCC 掉到 0.368、Perc15 MAE 10.03 HU。也就是说，**纯图像保真训练出来的模型基本没有把厚层的低估拉回来**；
护栏用 0.12 dB 的 PSNR 换来 LAA MAE 0.61→0.39 pp、CCC 0.368→0.875。这正是 SCTE-R 相对 DLS 的立论点。

**(2) 文档给的 `w_traj=0.5` 是对的，调小反而更差。**
曾试过 `w_traj=0.05`：在 `*_best` 快照上看起来最好（测试集 0.32 pp / CCC 0.859），但配 EMA 长跑后
验证指标随训练**单调恶化**（0.82 → 1.17 pp）。说明那个 0.32 是抖动里挑出来的幸运点，不是稳定水平。

**(3) 用 15 例验证集挑 `*_best` 不可靠，EMA 末轮权重更稳。**
同一次 EMA 训练：验证集选出的 best 在测试集上 0.47 pp / CCC 0.750，而**最后一轮 EMA 权重**是
0.39 pp / CCC 0.875。8mm 那次差距更大（best 0.65 pp vs 末轮 0.40 pp）。
因此交付权重取 EMA 末轮（未经任何留出指标挑选），`*_best.pt` 仅作参考保留。

### 4.5 局限与注意事项（下一阶段要接着处理的）

1. **CCC 0.875 未到 0.9**（验证集 0.891）。本队列 LAA-950 参考值集中在 0.1–4%，动态范围窄，CCC 对
   少数几例的偏差很敏感；扩大数据量（LIDC-IDRI/LUNA16 扩规模）或按肺气肿负荷分层采样应能改善。
2. **推理窗口放大后 CCC 会衰减**（MAE 不衰减）。`public.pt` 在 `(40,64,64)` 上训练，
   换更大的推理窗口测试（同一 50 例）：

   | 推理 patch | 厚层 LAA MAE | SCTE-R LAA MAE | CCC | Perc15 MAE（厚层→SCTE-R） |
   |---|---|---|---|---|
   | 40×64×64 | 0.67 pp | 0.39 pp | 0.875 | 18.28 → 2.84 HU |
   | 40×96×96 | 0.87 pp | 0.37 pp | 0.919 | 18.92 → 3.27 HU |
   | 40×128×128 | 0.84 pp | 0.46 pp | 0.821 | 19.18 → 3.19 HU |
   | 40×160×160 | 0.65 pp | 0.37 pp | 0.759 | 19.20 → 3.12 HU |
   | 40×192×192 | 0.79 pp | 0.56 pp | 0.688 | 19.21 → 3.04 HU |

   MAE 与 Perc15 在各尺寸都稳定优于厚层，但 CCC 从 0.875 单调降到 0.688——窗口越大越异质，
   模型在肺边缘/血管/胸壁附近的误差开始主导跨例排序。整卷推理建议按训练尺寸滑窗后聚合，
   而不是一次喂大块。（注：**放大窗口训练并不能解决这一点**，见 4.6a。）
3. **评估是 patch 级、不是整卷级**，且肺掩膜用的是 HU 窗口的粗略近似
   （`metrics._lung_mask`）。私有数据阶段应换成真正的肺分割（TotalSegmentator / lungmask）并做整卷统计。
4. **M1 的 w0 目前是"名义值 + 小 CNN 头"**，没有做论文里的过采样边缘响应 SSP 拟合（代码里 `TODO(research)` 已标注）；
   合成数据里 SSP 是已知的，所以这一阶段测不出 M1 的真实收益。
5. 合成厚层的噪声模型是"薄层噪声经算子平均 + 厚层读出噪声"的简化，未建模重建核相关的相关噪声/NPS 形状差异。

### 4.6 附加实验

**(a) 大窗口训练不解决 CCC 衰减。** 用 `--patch 40 128 128` 重训（同样 clip+EMA），
在 128×128 窗口上测试：LAA MAE 0.68 pp / CCC 0.763，**反而不如 64×64 训练的 `public.pt`
在同一窗口上的 0.46 pp / CCC 0.821**（厚层 0.84 pp）。而且它的验证曲线在 epoch 30 触底（0.54 pp）
后单调恶化到 ~0.99 pp。结论：这个规模的骨干（32 通道 / 6 残差块 / 34.7 万参数）在大窗口上
容量不足，扩窗口要同时扩容量，否则不如小窗口滑窗。

**(b) 跨层厚 8mm（交接文档 §3 的可选项，为下一阶段安阳 8mm 铺路）**：
`--downsample 8 --slice_fwhm 8.0` 单独训练，测试集（8mm 合成，50 例）：

| 权重 | LAA MAE | CCC | Perc15 MAE | PSNR |
|---|---|---|---|---|
| 厚层输入 | 0.67 pp | 0.021 | 21.65 HU | 27.31 dB |
| `public_8mm.pt`（EMA 末轮，交付） | **0.37 pp** | 0.730 | **4.83 HU** | 26.70 dB |
| 同配置无 EMA 末轮（`runs/T8_ds8.pt`） | 0.40 pp | 0.872 | 4.87 HU | 27.09 dB |

8mm 退化同样能被拉回来，但**比 5mm 不稳**：8 倍下采样下验证 LAA MAE 在 0.2–1.1 pp 之间来回跳，
EMA 也没完全压住。下一阶段用安阳 8mm 时建议先在真实配对上微调，别直接信这个权重的绝对值。

### 4.7 sim-vs-real：公开预训练权重能不能直接吃真实厚层？

RPLHR-CT 自带**真实** 5mm 系列，所以不用等私有数据就能量出 sim-to-real 落差。
`scripts/sim_vs_real.py` 对同一病例、同一 patch、同一权重（`public.pt`），只换厚层来源
（SSP 合成 vs 扫描仪真实 5mm），50 例官方 val：

| 厚层来源 | LAA MAE | LAA 中位误差 | LAA 偏倚 | LAA CCC | Perc15 MAE | PSNR |
|---|---|---|---|---|---|---|
| 合成厚层（输入） | 0.66 pp | 0.21 pp | −0.66 | 0.087 | 17.71 HU | — |
| **合成厚层 → SCTE-R** | **0.35 pp** | 0.18 pp | −0.34 | **0.897** | **2.86 HU** | 28.67 dB |
| 真实厚层（输入） | 0.67 pp | 0.21 pp | −0.67 | 0.029 | 20.99 HU | — |
| **真实厚层 → SCTE-R** | 0.57 pp | 0.20 pp | −0.57 | **0.470** | **3.56 HU** | 27.40 dB |

- **Perc15 迁移得很好**：真实输入下仍然 20.99 → 3.56 HU（−83%，与合成的 −84% 基本持平）。
- **LAA-950 只迁移了一部分**：合成输入能修回 47% 的误差、CCC 0.897；真实输入只修回 15%、CCC 0.470。
- z 对齐：50 例全部在 offset=0 匹配最好（RPLHR 的真实配对本来就是同一次原始数据的两种重建）。

含义：`public.pt` 直接上真实厚层，Perc15 类指标可用，**LAA-950 这条主线不能直接信**——
必须先在真实配对上微调（RPLHR train 有 100 对真实 5mm/1mm，可以先拿它排练），或者
把前向算子做实（真实 SSP 拟合 + 相关噪声/重建核建模）。

---

## 5. 交接给第二阶段

### 5.1 产物

| 路径 | 内容 |
|---|---|
| `public.pt` | **交付权重**（5mm/1mm，EMA 末轮），下一阶段 `--init public.pt` 用这个 |
| `public_8mm.pt` | 8mm/1mm 版本（可选，安阳 8mm 用） |
| `runs/*.pt` | 全部实验权重（含消融与 `*_best` 快照） |
| `results/summary.txt`、`results/eval_*.csv` | 所有评估输出与逐例指标（LAA/Perc15/PSNR/SSIM/reliability） |
| `DATA/public_thin{,_val,_test}` | 预处理好的薄层 `.npy`（85/15/50） |
| `DATA/rplhr/*/5mm` | RPLHR 真实 5mm（本阶段未用，留作下一阶段 sim-vs-real 对照） |

### 5.2 私有数据还差什么（`--data real` 目前跑不起来）

1. **`RealPairedDataset` 只是骨架**：`patch` 参数存了但从未使用，`__getitem__` 返回**整卷**
   （512×512×300 送进 3D 网络必然 OOM），代码内自带 `TODO: in-plane resample; z-align; lung mask`。
2. **`pair_audit.csv` 没人生成**：`scan_format.py` / `audit_pairs.py` 按交接文档不在本包内。
3. **z 整除约束**：模型固定 `D_thin = downsample × D_thick`。连 RPLHR 这种整理过的公开真实配对
   都是 261 层 vs 53×5=265 层；私有 DICOM（层厚/FOV/重建核/spacing 各异）只会更碎。
   需要在 dataset 里做 z 对齐 + 裁到整除 + 面内重采样到共同 spacing。
4. **sim-to-real 落差**（4.7）：LAA-950 这条主线在真实厚层上只修回 15%。

建议顺序：先用 RPLHR 的 100 对**真实** 5mm/1mm 把 (1)(3) 的数据通路写通并微调 `public.pt`，
在公开数据上把 4.7 那张表的 "real" 行拉起来；这条路走通了，私有数据到手就只是换数据源。

```bash
# 私有数据微调（等 scan_format.py / audit_pairs.py 与上述数据通路到位后）
python -m scte_r.train --data real --audit pair_audit.csv --root /path/cases \
       --init public.pt --epochs 20 --clip 1.0 --ema 0.999 --ckpt private.pt
```

### 5.3 建议

1. **`--clip 1.0` 请一直带着**：轨迹护栏的梯度量级比体素项大约三个数量级，不裁剪一定发散。
2. **选模型不要只看 `*_best`**，用 EMA 末轮或至少用"最后 N 轮均值"作为判据（4.4-3）。
3. **整卷推理按训练窗口滑窗**，不要一次喂大块；扩窗口需要同时扩模型容量（4.5-2、4.6a）。
4. 扩规模用 LIDC-IDRI（TCIA 的 `getSeries`/`getImage` REST 接口可直接脚本化拉取，1018 个 CT 序列）
   或 LUNA16（Zenodo），`prep_public.py --dicom` / `.mhd` 两条路径都已就绪。
5. 私有阶段把 `metrics._lung_mask` 换成真肺分割（TotalSegmentator / lungmask），并做整卷统计。

---

## 6. 追加：真实配对微调排练（RPLHR 真实 5mm/1mm）

§4.7 量出公开预训练权重在真实厚层上 LAA 只修回 15%。这一节用 RPLHR 自带的 **100 对真实
5mm/1mm** 把私有阶段那条路提前走一遍：数据通路、微调、以及"公开预训练到底有没有用"。

### 6.1 新增的数据通路（就是私有阶段要用的那套）

- `scripts/prep_pairs.py`：肺包围盒 → z 对齐（±4 层网格搜索最小化 `||A(thin) − thick||`）
  → 裁到 `D_thin = r × D_thick` 严格整除 → 存对齐好的 `<case>_thin.npy` / `<case>_thick.npy`，
  并写 `pair_audit.csv`（逐例 offset / 匹配 MSE / 肺占比）。50+100 例全部 offset=0（RPLHR 的
  真实配对本来就是同一次原始数据的两种重建），私有数据不会这么规整。
- `scte_r/datasets.py` 新增 `PreparedPairDataset`；`train.py` / `evaluate.py` 新增 `--data pairs`。
- 划分沿用合成阶段的同一份 case 列表：85 训 / 15 验 / 50 测（RPLHR 官方 val）。

### 6.2 先做归因：落差不是层厚，是纹理

`scripts/fit_ssp.py` 在 50 例真实配对上直接拟合有效层敏感度宽度（网格搜索最小化 `||A_w(thin) − thick||`）：

- **有效 FWHM 中位 6.25 mm**（范围 5.75–6.75），**落在训练时的 5–7 mm 抖动区间内** → 算子宽度不是问题
- 高斯 SSP 解释不了的残差 = 厚层信号 std 的 **12.8%** → 问题在相关噪声 / 面内重建核 / SSP 非高斯拖尾

### 6.3 微调结果（50 例真实配对测试集）

| 权重 | LAA-950 MAE | LAA 偏倚 | CCC | Perc15 MAE | PSNR |
|---|---|---|---|---|---|
| 真实厚层输入（无模型） | 0.66 pp | −0.66 | 0.030 | 20.98 HU | 27.63 dB |
| `public.pt` 零样本 | 0.56 pp | −0.56 | 0.472 | **3.58 HU** | 27.45 dB |
| **真实配对微调（`public_realft.pt`）** | **0.41 pp** | −0.41 | **0.840** | 4.23 HU | 27.30 dB |
| 同配置**从零训练**（无 `--init`） | 0.41 pp | −0.41 | 0.840 | 4.23 HU | 27.30 dB |

三个微调配置（lr 2e-4 / lr 1e-3 / 从零）在小数点后两位上给出**同一个结果**。

### 6.4 微调到底学到了什么：一个标量

把微调后的权重和 `public.pt` 逐张量比对，**只有 `m2.out.bias` 变了，改动量 −12.2 HU**，
其余 56 个张量的差异都在 float32 舍入噪声量级（总和 0.016，而权重总范数 7447）。
验证：拿 `public.pt` **只替换这一个标量**（`runs/public_biasonly.pt`），在 50 例真实配对上得到
**0.41 pp / CCC 0.840 / Perc15 4.23 HU —— 与完整微调完全一致**。

所以"真实配对微调"在这个数据集上等价于**一次全局 HU 重标定**，不是结构性的适配。对应的分布诊断
（50 例肺内统计，HU）：

| | std | mean |
|---|---|---|
| 薄层参考 | 58.6 | −853.9 |
| 真实厚层（上采样） | 50.8 | — |
| `public.pt` 重建 | 54.9 | −862.3 |
| 微调后重建 | 54.9 | −868.4 |

重建把厚层丢掉的纹理找回了一部分（50.8 → 54.9，参考 58.6），但**分布仍然偏窄**；微调没有加宽它，
而是把整个直方图再往下推 6 HU，让 −950 的穿越点平均对上。代价是 Perc15 MAE 3.58 → 4.23 HU，
在合成测试集上 Perc15 MAE 也从 2.84 退到 5.98 HU（LAA MAE 仍 0.39、CCC 0.875 → 0.842）。

### 6.5 结论与对下一阶段的影响

1. **好消息**：真实厚层上的 LAA 落差在本数据集上是**可标定的偏置**，不是结构性失效。
   CCC 0.472 → 0.840，已经接近合成域的 0.875。
2. **代价明确**：这个修正是拿 Perc15 和 PSNR 换来的，说明 M4 的 NPS/轨迹护栏在真实输入上
   **还没有真正复原低衰减尾部**，只是把直方图挪了位置。要真正解决，得把噪声/重建核建模做实
   （§6.2 的 12.8% 残差），或给 M1 显式加一个每序列的 HU 标定量。
3. **公开预训练在这一步没有体现出增益**：从零训练达到同样结果。原因是这一步的任务被那个标量主导，
   85 对真实配对足够学会。**但这不能外推到私有数据**——那里配对更少、更脏、跨中心跨设备，
   预训练权重的价值要在私有数据上重新评估（也可能正是它派上用场的地方）。
4. **给私有阶段的实操建议**：先用少量真实配对拟合每个"设备 × 协议"的标定量，再考虑全量微调；
   两者的差距（本实验里是零）就是全量微调的真实收益。注意本节结论只来自 RPLHR 一个中心的
   真实 5mm 重建，河北 5mm / 安阳 8mm 可能不只是偏置差异。

### 6.6 产物补充

| 路径 | 内容 |
|---|---|
| `public_realft.pt` | 真实 5mm 输入用（RPLHR 真实配对微调后） |
| `runs/public_biasonly.pt` | 只改输出偏置的对照权重（证明 6.4） |
| `DATA/public_pairs{,_val,_test}` | 对齐好的真实配对 + `pair_audit.csv` |
| `results/eval_realpairs_*.csv` | 逐例结果（零样本 / 微调 / 从零 / 仅偏置） |

---

## 7. 追加：ARC-CT —— 把 multi-agent 做成算法承重结构，以及它测出了什么

按 IdeaSpark 产出的 idea card 实现（`scte_r/agentic.py`）。三个 solver 各自拥有一个可独立证伪的估计量，
交换的是**可机器校验的类型化产物**而不是激活；仲裁器把分歧变成对"报出去的那个数"的判定：

| solver | 拥有的估计量 | 产出 |
|---|---|---|
| 算子求解器（无学习参数） | 有效层厚 w、噪声 σ | (w, σ, ρ)，ρ = 该物理模型解释不了的观测占比 |
| 重建器（学习） | 薄层体数据 x̂ | 以 (w, σ) 为条件，而非标称层厚 |
| 密度学求解器（无学习参数） | 直方图的位置与展宽 | δ̂（整体平移）、ρ_struct（结构残差）、s（纹理项） |
| 仲裁器 | — | certified / flagged(指出主导违反项) / abstain |

承重性质：**归一化 SSP + 平均池化对任意层厚都保持层均值**，所以残差的均值方向能在不知道真实层厚的
情况下单独识别出全局 HU 平移——前提是取均值的集合必须是**腐蚀后的肺区内部**（实测：不腐蚀时完美重建
被判 +6.9 HU 且随层厚摆动 9.4 HU；腐蚀后 −0.55 HU、摆动 0.04 HU）。

### 7.1 实现过程中被证伪的一条 card 主张：逐例识别算子不可行

| 拟合参考 | 得到的 ŵ |
|---|---|
| 真实薄层（金标准，推理时没有） | 6.00–6.50 mm |
| 三线性上采样的厚层 | **2.00 mm（网格下界）** |
| 模型自身的重建输出 | **2.00 mm（网格下界）** |

两个推理期可用的参考都是从 y 派生、纵向已平滑，再模糊一点就对不上 y——**循环依赖导致不可辨识**。
免参考的替代（厚层自身各向异性比）在合成臂上对 w 单调但精度仅 ~2.2 mm，且真实臂取值整体落在合成臂
范围之外。**改为 protocol 级识别**（85 对真实配对拟合：6.25 mm 中位、6.16 ± 0.51），每例仍报 ρ 作为
"这一例不像这个 protocol"的告警。方法之所以还成立，正是因为 δ̂ 对 w 不变。

### 7.2 阈值由"完美重建零分布"定，且只用训练集

`s`、`ρ_struct` 都依赖 patch 几何，绝对阈值会把 ground truth 判死。改为 τ = |mean| + 2·sd，在**训练集**上
用完美重建（x̂ := 真实薄层）测出：

| protocol | τ_δ | τ_ρ | τ_s |
|---|---|---|---|
| RPLHR 真实 5 mm（85 对） | 4.33 HU | 0.774 | 2.01 σ_v |
| 合成 8 mm（85 例） | 0.29 HU | 0.224 | n/a |

两行 τ_δ 相差 15 倍，这个差本身就是 sim-to-real 落差在证书单位下的度量。测试集上完美重建
**45/50 通过**（δ̂ = +1.04 ± 1.44 HU），即约 10% 的误报率，如实报告。

### 7.3 主结果：现有目标函数的密度学收益 = 直方图平移

50 例真实配对测试集：

| 臂 | δ̂ (HU) | LAA-950 MAE | CCC | Perc15 MAE | 通过 | 拦下它的项 |
|---|---|---|---|---|---|---|
| 厚层输入 | — | 0.66 pp | 0.030 | 20.98 HU | — | — |
| `w_traj=0` 消融 | −3.47 | 0.63 | 0.151 | 13.92 | 0/50 | displacement |
| `public.pt` 零样本 | −15.44 | 0.53 | 0.535 | 3.32 | 0/50 | displacement |
| 真实配对微调 | −21.54 | 0.36 | 0.876 | 4.52 | 0/50 | displacement |
| 3 对配对 + 1 标量 | −20.94 | 0.38 | 0.856 | 4.12 | 0/50 | displacement |
| ARC-CT 无惩罚 | −23.60 | 0.36 | 0.895 | 6.24 | 0/50 | displacement |
| **ARC-CT 带惩罚**（λ=0.1/0.3/1.0） | **−0.30** | 0.64 | 0.117 | 17.09 | 0/50 | **tail_recovery** |
| 标称层厚消融 | −0.37 | 0.64 | 0.117 | 17.01 | 0/50 | tail_recovery |
| 完美重建（真实薄层） | +1.04 | 0.00 | 1.000 | 0.00 | **45/50** | — |

**剂量-反应**：跨四个独立训练配置，位移越大、LAA 指标越"好"（−3.5 HU → CCC 0.151；−15.4 → 0.535；
−21.5 → 0.876；−23.6 → 0.895）。把位移按 HU 计价惩罚掉后，δ̂ 降到 −0.30 HU，**密度学收益随之全部消失**
（0.64 pp / CCC 0.117 ≈ 厚层输入水平），三个惩罚强度结果完全一致——不是调参问题。

**这不是 sim-to-real 现象**：`public.pt` 在自己最擅长的**合成 5 mm 域**（LAA MAE 0.37 pp / CCC 0.892）同样
携带 δ̂ = −15.56 ± 0.37 HU，而该域完美重建的零分布是 +0.01 ± 0.13 HU（相差约 120 σ），50/50 全被拒。
仅偏置臂随 N 的曲线也印证：N=3 时 −5.5 HU 给出 0.38 pp / CCC 0.856（优于 200 epoch 完整微调），
N=25 时拟合值翻到 +3.5 HU、指标退化——它拟合的是噪声，不是一个真实校正。

### 7.4 证书的诊断会正确翻转

flagged 的主导项分布：零样本 / 微调 / 仅偏置 / 无惩罚 ARC → **displacement ×50**；加惩罚后 →
**tail_recovery ×50**（s = +5.90 σ_v，重建的纵向方差远低于观测所蕴含的值）；完美重建 → 45 通过、
4 tail_recovery、1 structural。也就是说证书不止给通过与否，它指出"位移修好了但尾部仍没恢复"。

### 7.5 诚实的空结果与局限

- **算子识别在本设置下是 null ablation**：protocol 拟合层厚（6.25 mm）与标称值（5.0 mm）给出**完全相同**的
  终点指标（0.64 pp / CCC 0.117）。δ̂ 对 w 的不变性解释了这一点——这既是方法的优点，也意味着这一腿
  在 5 mm 上暂时没有可测收益；它的价值要到跨层厚/跨设备场景才可能兑现。
- **ARC-CT 现在是一个成立的审计器，而不是一个更好的重建器**：它能可靠区分"指标对了"与"指标对得有道理"
  （完美重建 45/50 通过，所有靠平移取胜的模型 0/50），但重建侧仍缺真正的尾部恢复能力
  （肺内 std 54.9 vs 参考 58.6 HU）。
- **8 mm 仅限合成臂**（无公开真实 8 mm 配对）：`public_8mm.pt` 在合成 8 mm 上 LAA MAE 0.40 pp / CCC 0.779，
  但同样携带 δ̂ = −14.44 HU，0/50 通过。
- 单位计价再次成为坑：δ̂ 惩罚若按 kHU 计价则比轨迹项小约 1000 倍、等于没加（实测 −16.6 vs −17.6 HU）。

### 7.6 产物

| 路径 | 内容 |
|---|---|
| `scte_r/agentic.py` | ARC-CT：OperatorSolver / DensitometrySolver / Arbiter / ARC_CT / SCTER_Iterative（`--iters>1` 的迭代变体） |
| `scripts/fit_protocol_operator.py` | protocol 级层厚 + 纹理项标定（含"无信息量则拒绝发布"规则） |
| `scripts/certify.py` | 逐例证书 + 终点指标联合评测（缺证书不许报终点），含 oracle 自检与仅偏置对照臂 |
| `runs/protocol_rplhr_5mm.json` · `runs/protocol_sim_8mm.json` · `runs/protocol_sim_5mm.json` | 三个 protocol 的标定与阈值 |
| `results/cert_*.csv` | 全部臂的逐例证书 |
| `ideaspark_run/multiagent-thick-slice-densitometry/` | idea card（中/英/审稿人版）+ `ADDENDUM_implementation_findings.md`（实现阶段的修正记录） |

### 7.7 追加：种子复现 + 分布匹配损失（两个都是负面结果，但都很干净）

**(a) 种子复现**（同配置换 seed 重训，50 例真实配对测试集）：

| 配置 | seed 0 | seed 1 |
|---|---|---|
| 无位移惩罚 | δ̂ −23.6 HU，LAA 0.36 pp / CCC 0.895 | δ̂ −18.2 HU，LAA 0.47 pp / CCC 0.706 |
| 带位移惩罚 | δ̂ −0.30 HU，LAA 0.64 pp / CCC 0.117 | δ̂ −0.45 HU，LAA 0.64 pp / CCC 0.118 |

堵住平移后的结果在两个种子上几乎逐位一致；不堵时位移幅度与指标同向变化。**机制不是种子噪声。**

**(b) 分布匹配损失**：把 LAA 代理换成肺内直方图的分位数匹配
（分位点 1/5/10/15/25/50/75/90%，按 HU 计价）。动机是数学上的——整体平移会把所有分位数
同时移动同样的量，因此**分位数匹配无法被平移满足**（实测：对一个 +12 HU 的平移副本，该损失
精确报 12.0 HU，而 LAA 代理对它几乎无感）。

| 臂 | δ̂ | LAA MAE | CCC | Perc15 MAE | 尾部项 s |
|---|---|---|---|---|---|
| 只惩罚位移 | −0.30 HU | 0.64 pp | 0.117 | 17.09 HU | +5.90 σ_v |
| + 分位数匹配（保留 traj，w_quant 0.1） | −0.45 | 0.64 | 0.118 | 16.94 | +5.90 |
| + 分位数匹配（替掉 traj，w_quant 0.1） | −0.37 | 0.64 | 0.117 | 17.02 | +5.90 |
| + 分位数匹配（替掉 traj，w_quant 0.3） | −0.67 | 0.64 | 0.120 | 16.71 | +5.90 |

**分位数匹配确实堵死了平移，但没有换来任何真实的尾部恢复。** 最能说明问题的是最后一列：
**尾部项 s 在所有臂上锁死在 +5.90 σ_v**，换损失、换权重、换种子都不动一位小数——这些目标函数
从未把低衰减尾部动过分毫，它们唯一能改变的是直方图的位置。

**结论与下一步**：在当前网络容量（0.35 M 参数的确定性 3D 残差网络）与这一类逐体素/统计量损失下，
5 mm→1 mm 的低衰减尾部信息是恢复不了的；文献中这条线的 LAA 收益全部来自平移。要真正翻案需要的
不是再换一个损失，而是**能合成纹理的生成式解码器**（flow / diffusion，对标 arxiv:2607.16649v1 DRIFT）
——这也正是论文里"为什么必须走生成式"的实证依据，而不是审美偏好。

### 7.8 生成式解码器：第一个把密度学精度「挣来」而不是「换来」的方法

§7.7 的负面结论指向一个具体假设：**确定性回归无法合成纹理**，因此在逐体素/统计量损失下，
给定平滑的厚层，L1/L2 最优解就是平滑解——换哪个损失都改不了。于是把重建器换成**采样器**：
`scte_r/flow.py`，在**残差空间**做条件流匹配（`u = (x_thin − upsample(y))/s_r`，
从 N(0,I) 用 Euler 积分到 u，再做数据一致性投影）。残差空间是关键：目标零均值、尺度约 50 HU，
噪声先验与要合成的量同尺度；若从 N(0,1) 一路流到均值 −0.88 kHU 的分布，每一步都在搬运均值，
而均值恰恰是证书说本来就没问题的那个量。

训练目标只有流匹配损失，**不含任何 LAA 项**，因此结构上没有平移的动机；证书全程只测不训。

50 例真实配对测试集，300 epoch：

| 臂 | δ̂ | LAA MAE | CCC | Perc15 MAE | PSNR | 尾部项 s | 证书通过 |
|---|---|---|---|---|---|---|---|
| 靠平移（无惩罚，终点最好的确定性臂） | −23.60 HU | 0.36 pp | 0.895 | 6.24 HU | 27.24 dB | +5.88 | 0/50 |
| 堵死平移的确定性臂（6 个配置全同） | −0.30 HU | 0.64 pp | 0.117 | 17.09 HU | 27.61 dB | +5.90 | 0/50 |
| 流匹配 s_r=0.020 | −0.43 | 0.39 | 0.617 | 7.87 | 28.24 | +2.67 | 4/50 |
| **流匹配 s_r=0.028** | −0.79 | **0.36** | 0.714 | 6.34 | 28.41 | +2.24 | 12/50 |
| 流匹配 s_r=0.035 | −0.61 | 0.37 | 0.670 | 6.29 | 28.42 | +2.17 | 16/50 |
| 流匹配 s_r=0.042 | −0.65 | **0.36** | 0.695 | 5.85 | 28.44 | +2.13 | 16/50 |
| **流匹配 s_r=0.050** | −0.39 | 0.37 | 0.674 | **5.94** | **28.57** | **+2.11** | **19/50** |
| 完美重建（上界） | +1.04 | 0.00 | 1.000 | 0.00 | — | ~0 | 45/50 |

三条结论：

1. **同样的 LAA 精度，不靠平移拿到**：0.36 pp 与靠平移的臂完全相同，而 δ̂ 从 −23.6 HU 降到 −0.7 HU。
2. **尾部第一次被动了**：s 从所有确定性臂锁死的 +5.90 走到 +2.11（约 2/3 的距离），证书通过率
   0/50 → 19/50，且随 `s_r` 单调上升；PSNR 也是所有臂最高（28.6 vs 27.2–27.6）。
3. **训练早期就能看到 `s_r` 的两端**（epoch 6：s_r=0.02 纹理不足 s=+4.18；s_r=0.05 纹理过量、
   LAA overshoot 到 2.23 pp 但证书放行 42/50），充分训练后两端收敛到同一 LAA 水平、
   仅在证书通过率上保留差异——说明 `s_r` 控制的是纹理合成的强度先验，不是精度旋钮。

**试过并否掉的一条修法**：对 K 次采样取平均。LAA 是泛函，其后验均值才是正确估计量（对图像取平均
会把纹理重新抹平），但实测 K=1/4/16 给出相同的 0.36 pp / CCC 0.70——**剩余误差是逐例系统偏差
而非采样方差**，加采样次数无效。

**仍未解决**：CCC 0.67–0.71 低于靠平移臂的 0.895（后者的高 CCC 本身是平移的产物——CCC 惩罚偏倚，
该臂能到 0.895 说明那个 −23.6 HU 的平移恰好帮了跨例相关），证书通过率 19/50 也远低于完美重建的
45/50。也就是说：**这是"第一个挣来精度的方法"，不是"问题解决了"。**

**必须分开声明的两件事**：certified ≠ accurate。s_r=0.05 在早期 checkpoint 上证书放行 42/50，
但那时 LAA MAE 是 2.26 pp——证书证明的是「这个数有测量证据支持」，不是「这个数准」。

---

## 8. 口径更正与最终结果（TotalSegmentator 肺叶掩膜）

### 8.1 必须先声明的一处评测错误

§4–§7 的 LAA-950 全部用 `metrics._lung_mask`（HU 窗 `(-990, -500)`）作分母。装上
TotalSegmentator 后逐例比对发现，这**不是临床 LAA-950**：

| 掩膜 | 同一例的 LAA-950 |
|---|---|
| TotalSegmentator 五肺叶 | **0.34 – 1.57 %** |
| HU 窗 `(-990,-500)` | **9.9 – 13.6 %** |

原因：HU 窗的分母本身就是低密度窄带，"带内低于 −950 的比例"被严重抬高；临床 LAA-950 的分母是
**解剖肺实质**（含血管、含 < −990 的气道与重度肺气肿区）。两者重叠率 TS∩HU/TS = 0.849、/HU = 0.950，
即 HU 窗漏掉约 15% 的真肺又混入非肺低密度区。

**影响范围**：各臂之间的相对比较不受影响（同一错误口径对所有臂一致），机制结论（平移换指标、
生成式挣来精度）全部成立；但**旧口径下的绝对值作废**，包括此前"RPLHR 参考 LAA 中位 0.25%"这句
——那是错口径的数。§8.2 起全部用 TS 掩膜重算。

### 8.2 最终结果（50 例独立测试集，TS 肺叶掩膜，patch 级）

| 臂 | LAA-950 MAE ↓ | CCC ↑ | Perc15 MAE ↓ | PSNR ↑ | δ̂ | 尾部 s | 证书通过 |
|---|---|---|---|---|---|---|---|
| 厚层输入 | 0.65 pp | — | — | — | — | — | — |
| 经典 Landweber 反卷积 | 3.81 | 0.139 | 3.29 | 25.56 | **−0.00** | +5.9 | 0/50 |
| `public.pt` 零样本 | 0.52 | 0.554 | **3.14** | 27.44 | −15.62 | +5.89 | 0/50 |
| 真实配对微调 | 0.35 | **0.881** | 4.72 | 27.30 | **−21.72** | +5.88 | 0/50 |
| 堵死平移（确定性，6 配置同） | 0.63 | 0.120 | 16.77 | 27.61 | −0.45 | +5.90 | 0/50 |
| **流匹配 0.35M，64 步** | **0.34** | 0.852 | 4.57 | **28.15** | **−0.55** | **+1.49** | **42/50** |
| 流匹配 ch48/b8，64 步 | 0.34 | 0.839 | 4.78 | 28.29 | −0.67 | +1.68 | 37/50 |
| 流匹配 ch64/b10，64 步 | 0.34 | 0.857 | 4.71 | 28.08 | −1.15 | +2.05 | 20/50 |
| 完美重建（上界） | 0.00 | 1.000 | 0.00 | — | +1.04 | −0.45 | 45/50 |

**流匹配解码器在正确口径下全面领先**：LAA MAE 0.34 优于靠平移的 0.35，PSNR 28.15 优于 27.30，
CCC 0.852 基本追平 0.881，而 δ̂ 只有 −0.55 HU（对方 −21.72），证书 42/50 对 0/50，逼近上界 45/50。

### 8.3 两条干净的负面结论

1. **扩容量无效**：0.35M → 1.2M（ch48/b8）→ 2.6M（ch64/b10），LAA 全部停在 0.34 pp，
   证书通过率反而从 42 掉到 20。**收益来自采样步数**（16→64 步：CCC 0.714→0.852，证书 12→42），
   不是参数量。这对方法叙事是好消息：机制在于"必须采样"，不是"必须更大"。
2. **测量引导（每步 DC 梯度）适得其反**：guidance 0→0.6，证书三项越来越漂亮
   （g=0.15 时通过 46/50，比完美重建还高），**但 LAA 从 0.34 崩到 4.95 pp**。原因是真实薄层并不
   精确满足 `A_w(x)=y`（高斯 SSP 解释不了的残差占 12.8%），把错的前向模型当硬约束会把解拽偏。
   同一现象在加大数据一致性投影步数时重现（0.36→0.75→1.48 pp）。

   **推论**：当前前向算子只够检测"全局平移"这类粗糙泛函，不够用来引导精细结构。下一步真正的杠杆是
   **把前向算子学出来**（`A_θ = A_w + f_θ`，前向问题良定，85 对配对足够），而不是继续调采样器。

3. **certified ≠ accurate 有了硬证据**：g=0.15 那一行证书通过 46/50 而 LAA MAE 3.35 pp——证书约束
   均值方向与二阶矩，样本可以矩对而位置错。必须分开声明。

### 8.4 新增产物

| 路径 | 内容 |
|---|---|
| `scte_r/flow.py` | 流匹配解码器（残差空间、可选测量引导），`build_model(flow=True)` |
| `scripts/make_lung_masks.py` | 批量 TotalSegmentator 肺叶掩膜（`<case>_lung.npy`，0/1–5 叶） |
| `scripts/certify.py --lung_mask --per_lobe` | 按解剖掩膜计算指标与证书 |
| `runs/FL*.pt` | 五个 residual_scale 的流模型 + 两个大容量版本 |
| `results/final_*.csv` | 正确口径下的逐例最终结果 |

## 9. 学出来的前向算子、全肺推理、扩展指标与图

§8.3 结尾把"把前向算子学出来"列为下一步杠杆，本节是它的执行结果，外加两件把结论从
patch 级抬到临床级的工作：整卷滑窗推理、以及 LAA-910/Perc10/Perc5/逐肺叶指标。

### 9.1 学出来的前向算子 `A_θ`

`scte_r/forward_learned.py`：`A_θ = Σ_w softmax(α)_w · A_w + f_θ`，其中 `A_w` 是宽度
w ∈ {3,4,5,6,7,8,10,12} mm 的高斯 SSP + 按 r 跨步平均，`f_θ` 是一个**逐层零均值**的 CNN 修正。
零均值约束是必须的：它保证 `A_θ` 仍然保持均值/板层守恒，因此 §7 的 δ̂ 估计量（以及整套证书）
在换成学习算子后**定义不变**，不需要重新校准 null。

在 85 对真实配对上拟合（前向问题良定，直接监督 `A_θ(x_thin) ≈ y_thick`）：

| | 未解释残差 `‖A(x)−y‖/‖y−ȳ‖` |
|---|---|
| 单高斯 w = 6.25 mm（§7 的协议级拟合） | 0.3563 |
| 拟合剖面 + 零均值修正 | **0.1612（−55%）** |

拟合出的剖面是**窄核 + 重尾**：5 mm 权重 0.847，而 w ≥ 6 mm 的尾部合计 0.135，
等效 FWHM 5.40 mm。这解释了 §8.3 里"高斯 SSP 解释不了 12.8% 残差"的来源——真实层敏感度
剖面不是高斯，尾巴比高斯厚，这部分正好落在肺气肿最关心的低密度端。

**换上 `A_θ` 后流匹配解码器（50 例测试集，TS 掩膜，patch 级）**：

| | LAA-950 MAE | CCC | 证书通过 |
|---|---|---|---|
| 高斯算子（§8.2） | 0.34 pp | 0.852 | 42/50 |
| 学习算子 | **0.31 pp** | **0.871** | 41/50 |

**一处对我自己假设的更正**：§8.3 我推测测量引导之所以有害是因为前向算子错了。换上残差减半的
学习算子后**引导仍然有害**，所以那个解释不成立。真正的原因是训练/推理不匹配——速度场是在
无引导轨迹上训练的，推理时每步注入 DC 梯度就把状态推离训练分布；算子更准只是让它更快地跑偏。
这条负面结论保留，但解释换了。

### 9.2 整卷滑窗推理（`scripts/infer_volume.py`，10 例，TS 五肺叶掩膜）

patch 级指标会低估临床误差，因为真正上报的是**全肺一个数**。整卷结果：

| 指标 | 厚层输入 | 真实配对微调（靠平移） | 流匹配（ours） |
|---|---|---|---|
| LAA-950 偏差 (pp) | −0.49 | +0.22 | **−0.19** |
| LAA-910 偏差 (pp) | −7.58 | +5.52 | **−2.25** |
| Perc15 偏差 (HU) | +19.38 | −8.49 | **+5.68** |
| Perc10 偏差 (HU) | +21.52 | −6.92 | **+5.98** |
| LAA-950 CCC | 0.151 | 0.729 | 0.722 |
| LAA-910 CCC | 0.107 | 0.639 | **0.838** |
| Perc15 CCC | 0.541 | 0.849 | **0.920** |

读法要小心**两族指标的符号语义相反**：LAA 为负 = 报少了肺气肿；百分位为正（HU 更高 = 更致密）
= 也报少了肺气肿。所以厚层输入四个指标一致地**低估**肺气肿（−0.49 / −7.58 / +19.38 / +21.52），
靠平移的微调臂把每一个都**翻到了另一侧**（+0.22 / +5.52 / −8.49 / −6.92）——它不是把误差消掉，
是把整条 HU 分布推过头；流匹配则是**同侧收窄**（−0.19 / −2.25 / +5.68 / +5.98），符号与厚层一致
而幅度减半到三分之一。这是"平移 vs 真的恢复"在全肺尺度上最干净的一张对照。

### 9.3 扩展指标与逐肺叶

`scripts/certify.py` 现在同时输出 LAA-950 / LAA-910 / Perc15 / Perc10 / Perc5，
`--per_lobe` 输出五个肺叶各自的 LAA。50 例采样步数扫描（TS 掩膜）：

| 步数 | LAA950 MAE | CCC | LAA910 MAE | P15 | P10 | P05 | \|δ̂\| | 证书 | PSNR |
|---|---|---|---|---|---|---|---|---|---|
| 8 | 0.46 | 0.573 | 4.51 | 8.37 | 9.42 | 11.27 | 2.02 | 0/50 | 28.76 |
| 16 | 0.37 | 0.751 | 3.45 | 6.44 | 7.16 | 8.43 | 1.82 | 11/50 | 28.50 |
| 32 | 0.32 | 0.837 | 2.86 | 5.33 | 5.87 | 6.78 | 1.69 | 35/50 | 28.33 |
| 64 | 0.31 | 0.871 | 2.58 | 4.78 | 5.22 | 5.97 | 1.60 | 41/50 | 28.23 |
| 128 | 0.31 | 0.884 | 2.46 | 4.50 | 4.90 | 5.61 | 1.57 | 42/50 | 28.18 |

五个指标**同向单调改善**，而 PSNR 反而单调下降（28.76 → 28.18）。这条是本项目最该拿出去讲的
一条曲线：**逐体素保真度和密度分布保真度在这里是对立的**，只报 PSNR/SSIM 的方法在这个任务上
优化的是错的东西。64 步是拐点（再翻倍只买到 CCC +0.013，PSNR 再掉 0.05），故部署默认 64 步。

逐肺叶 LAA-950 偏差（10 例整卷）：

| | UL_L | LL_L | UL_R | ML_R | LL_R |
|---|---|---|---|---|---|
| 厚层输入 | −0.33 | −0.50 | −0.32 | −0.80 | −0.77 |
| 真实配对微调 | −0.04 | **+0.45** | −0.04 | **+0.20** | **+0.53** |
| 流匹配（ours） | −0.12 | −0.16 | −0.13 | −0.29 | −0.32 |

下叶（LL_L / LL_R）和右中叶是厚层最吃亏的地方（−0.50 / −0.77 / −0.80），符合预期：下叶层厚方向
结构变化最快。平移臂在这三个叶上**过冲成正的**，流匹配则一致地把幅度压到厚层的三分之一左右且不变号。

### 9.4 图（`scripts/make_figures.py`，house style 见 `scripts/figstyle.py`）

| 文件 | 内容 |
|---|---|
| `figures/fig1_wholelung_bias.{pdf,png}` | 全肺偏差双面板（pp / HU 分开，因为两族符号语义相反），三臂对照 |
| `figures/fig2_displacement_and_steps.{pdf,png}` | 左：\|δ̂\| vs CCC 剂量反应（marker 面积 ∝ 证书通过率）；右：采样步数 vs CCC / 证书 |
| `figures/fig3_forward_operator.{pdf,png}` | 拟合 SSP 基权重（窄核 + 13.5% 重尾）与未解释残差 0.356 → 0.161 |

### 9.5 新增产物

| 路径 | 内容 |
|---|---|
| `scte_r/forward_learned.py` | `LearnedOperator`（softmax 宽度基 + 零均值 CNN 修正）、`effective_fwhm()` |
| `scripts/fit_forward_operator.py` | 在真实配对上拟合 `A_θ`，输出权重与残差 |
| `scripts/infer_volume.py` | 整卷滑窗推理 + 全肺/逐叶指标 |
| `scripts/certify.py --learned_op` | 用学习算子做识别与证书 |
| `scripts/figstyle.py`, `scripts/make_figures.py` | 出版风格图 |
| `results/volume_*.csv`, `results/steps_*.csv` | 整卷与步数扫描逐例结果 |

## 10. 队列范围更正（2026-08-21）

前面 §2/§4/§5 里把 8mm 分支写成"为安阳 8mm 铺路"，这条已经作废：**实际能拿到的只有
河北大学附属医院和河南省人民医院两家，都是 1mm + 5mm**。没有第三家队列，也没有 8mm 队列。
相应地：

- `public_8mm.pt` 与 §4(b) 的 8mm 结果**只在合成域成立**，是一个"层厚可推广性"的旁证，
  不是任何真实队列的准备工作。不要在论文里把它写成跨设备结果。
- **没有第三方队列可以做"与医院金标准 LAA 比对"这一环**。这意味着临床效度只能靠
  河南 491 例的外部验证 + 逐例证书来支撑，不能靠"我们的数和医院报的数一致"这句话。
  这反过来提高了证书的分量：它是手上唯一的逐例、无参考的效度证据。

队列分工：

| 队列 | 例数 | 角色 | 允许拟合什么 |
|---|---|---|---|
| 河北大学附属医院 | 203 | 内部训练 | protocol 常数、前向算子、证书阈值、模型权重 |
| 河南省人民医院 | 491 | **外部验证** | 什么都不许拟合，只测 |

`scripts/run_private.sh` 的第 4 个参数就是为这个设计加的：
`bash scripts/run_private.sh full /data/hebei hebei /data/henan`。

### 10.1 病情严重度分层：聚合数字掩盖了方法真正的收益

失去第三方队列后，"方法在临床上有用"只能靠严重度分层来撑。把 50 例测试集按 1mm 参考
LAA-950 三分位分层（分位点 0.10% / 0.58%）：

| 层 | n | 参考 LAA-950 | 厚层输入 | 零样本 | 真实配对微调（靠平移） | 流匹配（ours） |
|---|---|---|---|---|---|---|
| 轻（近正常） | 17 | 0.04 % | **0.04** | 0.04 | 0.04 | 0.08 |
| 中 | 16 | 0.29 % | 0.29 | 0.26 | **0.21** | 0.23 |
| 重 | 17 | 1.59 % | 1.58 | 1.24 | 0.80 | **0.63** |

（单位均为 LAA-950 MAE，pp）

三件事：

1. **聚合的 0.31 pp 被稀释了**。三分之一的病例参考 LAA 只有 0.04%，任何方法在这层都无事可做，
   把它们平均进去会同时压低所有臂的数字，掩盖真正的差距。
2. **收益随病变加重单调放大**。重度层里流匹配把误差降到厚层的 40%（1.58 → 0.63），
   比靠平移的臂再好 21%（0.80 → 0.63）；中度层两者接近；轻度层流匹配**反而略差**
   （0.08 vs 0.04）——没有东西可恢复时，采样注入的是噪声而不是信号。
3. **这是对"公开数据疾病负荷太轻"这一质疑的直接回应**。RPLHR 测试集参考 LAA-950 均值
   0.65%、中位 0.24%。目标队列的肺气肿负荷预计明显更高（**上机第一件事就是实测这个分布**，
   在此之前不要把任何具体倍数写进论文）。分层趋势说明：**目标队列越重，方法的相对收益越大**，
   而不是越小。

⚠️ 这仍然是外推：本层最重的一档均值 1.59%，离 8.22% 还有距离。但趋势是单调的且方向明确。
上机后第一件该做的分析就是在河南 491 例上重做这张表——那里三分位的跨度会大得多。

**一个由此产生的部署建议**：轻度层流匹配不如不做，说明应该按 δ̂ 与厚层 LAA 做前置分流——
厚层 LAA 已经接近 0 的病例直接沿用厚层结果，把采样预算留给有病变的病例。


### 10.2 与既有内部报告的边界（重要）

组内此前有一份关于同一课题的内部汇报材料。它的作用**仅限于**：当本路线走不通时，作为
"换个方向"的参考。因此：

- **不照搬其方法**。特别是"把 LAA/Perc 直接写进多任务损失"那条路线——§7 已经证明它的最优解
  是全局 HU 平移，本项目走的是相反的路（证书把平移标出来，生成式解码器不靠平移）。
- **论文中不提及、不引用该材料，也不使用其中的任何数字**。本报告里凡是来自该材料的数量
  （目标队列的 LAA 分布、既有方法的 PSNR/LAA 表），一律只作内部参考，且已在上文标注为
  "上机后实测"。所有进论文的数字必须出自本仓库可复现的实验。
- 队列分工（内部训练 / 外部验证）是常规的多中心设计，不构成引用义务。

**由此产生的硬性要求**（本项目要自己站住的四条）：

1. 在**自己的评测协议**下超过基线 —— 不能只和 trilinear 比，必须有完整的插值族
   （nearest / linear / cubic / Lanczos）与学习类基线，全部在同一协议下重测。
2. **确实精确重建了 1mm** —— 图像质量（PSNR / SSIM，全体积与肺区）必须同时好，
   不能只赢密度学指标。
3. **multi-agent 是承重的**（§7：算子求解器 / 密度学求解器 / 尾部标定 / 仲裁器），
   不是包装。
4. **外部验证队列上同样好**，且算法本身有创新性（位移机制 + 逐例证书 + 生成式解码器）。

## 11. 对齐修复后的完整重做（2026-08-25/26）

§9–§10 的所有数字来自一个**错位的数据准备**，本节全部作废并重做。

### 11.1 数据准备的对齐 bug

`prep_pairs.py` 的 `z_offset` 只在**整层厚层**单位上搜索偏移，小于一个 5 mm 层的错位在原理上看不见。
公开队列因此系统性地错了 **2 个薄层**。150 例（85 训练 / 15 验证 / 50 测试）**一致**选择
thick+1 / thin+3，前向算子残差 0.1723 → 0.0782（**−55%**）。

后果分布不均，这是它最危险的地方：

| | 影响 |
|---|---|
| 插值基线 | 被压低 **3.07 dB**（cubic 27.15 → 30.22） |
| 我方前向算子 / DC 投影 / 证书 / δ̂ | 全部在错位下运行 |
| TVSRN / CTHNet / I3Net | 上游约定 thick k ↔ thin 5k **恰好吻合错位数据**，未受罚 |

即"我方受罚、对方不受罚"。修法：`z_offset` 改为**薄层分辨率搜索**（已进 `prep_pairs.py`，
私有数据同样受益——两家医院的 1mm/5mm 独立重建，没有理由落在同一网格上）；
已准备好的数据用 `scripts/realign_pairs.py` 精确重切（只重切，不插值）。

**连带作废**：§9.1 的"5 mm 标称 → 实测 6.25 mm"。对齐后解析拟合 3.04 mm、学习算子等效 FWHM 5.08 mm。

### 11.2 三个已发表基线（官方实现，本仓库 `baselines/`）

TVSRN（MICCAI 2022）、CTHNet（npj Digital Medicine 2024）、I3Net（IEEE TMI 2024），
模型定义逐字未改，在**我们的划分上训练**、**我们的协议下评测**（50 例整卷，TS 肺叶掩膜）：

| 臂 | PSNR | 肺PSNR | SSIM | LAA950 | **LAA950 CCC** | LAA910 | P15 |
|---|---|---|---|---|---|---|---|
| nearest | 28.81 | 26.38 | 0.7669 | 0.95 | 0.049 | 7.75 | 18.32 |
| cubic | 30.24 | 27.61 | 0.7857 | 0.84 | 0.185 | 6.73 | 16.78 |
| Lanczos-3 | 30.27 | 27.65 | 0.7865 | **0.78** | **0.259** | 6.46 | 16.34 |
| TVSRN | 33.17 | 31.08 | 0.8375 | 0.90 | 0.068 | 7.27 | 15.80 |
| CTHNet | **33.44** | **31.60** | **0.8497** | 0.82 | 0.152 | 5.90 | 11.98 |
| I3Net | 33.22 | 31.34 | 0.8469 | 0.86 | 0.100 | 5.89 | 11.88 |

**核心发现（三份独立证据）**：三个 SOTA 各自比插值高 3.4–4.0 dB 肺 PSNR，
**密度学一个都没改善，LAA-950 的 CCC 全部低于 Lanczos 插值**（0.068 / 0.152 / 0.100 vs 0.259）。
全肺偏差更直接：cubic −0.84、TVSRN −0.90、CTHNet −0.82 pp——**多花 3.4 dB 等于没动**。
这不是某个架构的问题，是这一类方法的共性。

### 11.3 位移不是某个损失造成的，是确定性回归的通解

按已发表配方实现"生物标志物多任务损失"（LAA-950/910 + Perc15/10/5，原生分辨率直接约束，
无模糊阶梯）后，在对齐数据上测：

| 臂 | \|δ̂\| HU | 尾部 s | 证书 |
|---|---|---|---|
| 生物标志物损失 w=1.0 | 19.08 | 6.14 | 0/50 |
| 生物标志物损失 w=0.3 | 19.09 | 6.14 | 0/50 |
| 监督回归（轨迹损失） | 21.34 | 6.14 | 0/50 |
| 流匹配 | **0.48** | 1.67 | **40/50** |
| CTHNet + 流匹配 | 见 §11.4 | | **47/50** |

**w_bio 砍掉三分之二，δ̂ 纹丝不动（19.08 → 19.09）。** 所以此前"把 LAA 写进损失会导致平移"
这个说法**不准确**，正确的结论更强：

> 这个任务上，**任何确定性回归的最优解都是一个约 20 HU 的平移解**，与损失配方无关。
> 只有把重建器换成采样器，δ̂ 才落到 0.5 HU。

### 11.4 最终方法：强骨干 + 流匹配残差，一次推理两个输出

`x̂ = CTHNet(y) + s_r·u`，`u` 由流匹配采样，注入限制在从**骨干重建**（非参考）导出的肺掩膜内。

三个失败的调参假设，一并记录：**s_r 尺度匹配**（28→12 只值 0.05 dB 且劣化 LAA910）、
**证书后验校正**（四种全部变差，残差 82% 是逐例方差）、**注入区域限制**（+0.58 dB，方向对但量级小）。
结论是这不是调参问题：采样在 MSE 上必然输给条件均值，这是失真-感知权衡。

正确解法是同一模型给两个输出（6 例先导）：

| 输出 | PSNR | 肺PSNR | SSIM | LAA950 | CCC950 | LAA910 | P15 |
|---|---|---|---|---|---|---|---|
| CTHNet 骨干（对照） | 34.09 | 32.39 | 0.8735 | 0.36 | 0.275 | 5.93 | 11.22 |
| **4 采样均值（看图用）** | 31.39 | **32.19** | 0.8649 | 0.38 | 0.240 | 5.09 | 10.32 |
| **单采样（报指标用）** | 30.98 | 30.99 | 0.8498 | **0.13** | **0.857** | **2.04** | **5.06** |

看图那一路**肺 PSNR 距 SOTA 仅 0.20 dB**；报指标那一路**密度学是 SOTA 的 2.8–3.1 倍准**，
且证书 47/50 通过。两个输出来自同一次积分，不是两个网络。

⚠️ 上表为 6 例先导，全部 50 例的确认在跑。

## 12. 部署目标改变后的重新定位（2026-08-29 起）

课题目标由"定量标志物恢复"改为**面向乡镇医院的诊断辅助**（无 1mm 设备，输出交医生阅片，
最终以医生打分评判病灶可检出性）。以下按新目标重测，结论与 §11 不冲突但优先级完全不同。

### 12.1 病灶级检出：现状 vs 重建

固定一个"笨"检测器（肺内 > 阈值 HU、8–1000 体素的连通域），只换重建，真实 1mm 为真值。
检测器越笨越好——它不能有自己的偏好，否则分不清是重建的问题还是检测器的问题。

FROC 式判据（中心点落入参考病灶即命中，肺结节 CAD 的标准做法），thr = −400 HU，n=8：

| 臂 | 灵敏度 | 假阳/例 |
|---|---|---|
| 上采样（**乡镇医院现状**） | 57.9% | 667.6 |
| CTHNet（已发表） | 70.1% | **548.8** |
| CTHNet + 流匹配（ours） | **71.5%** | 709.5 |

⚠️ 这些"病灶"绝大多数是血管斜切面，不是结节，绝对值无临床含义，**只能横向比**。
真正的病灶级证据要靠 LUNA16（1186 个医生标注结节）和私有队列。

### 12.2 幻觉审计：生成式并没有显著更能虚构

宽松判据（重建的灶所在位置，参考里有无致密结构），thr = −400：

| 臂 | precision | recall |
|---|---|---|
| 上采样 | 99.93% | 89.6% |
| CTHNet | 99.88% | 96.7% |
| 流匹配 4 采样 | 99.80% | 98.0% |
| 流匹配 单采样 | 99.42% | **98.3%** |

虚构率从 0.13% 升到 0.58%，而漏检从 10.4% 降到 1.7%。**生成式的风险没有兑现。**

一处自我更正：初版判据在两个体积上**独立**做尺寸过滤，同一结构可以在参考侧超上限被剔除、
在平滑后的重建侧保留，于是同时被记成一次虚构和一次漏检。那一版给出 21.5%/23.2% 的虚构率，
是**测量伪影**。改为与原始超阈掩膜比对后才是上表的数。

### 12.3 局部证书：证伪

把 δ̂ 和数据一致性残差按 40×64×64 块计算，检验它们能否标出虚构灶所在的块（607 块）：

| 指标 | 干净块 | 出问题块 | 比值 |
|---|---|---|---|
| \|δ̂\| | 2.474 | 2.414 | **0.98×** |
| 数据一致性残差 | 0.062 | 0.071 | 1.15× |

取最高 10% 的块，覆盖虚构灶 12.0%（δ̂）/ 6.7%（DC），**与随机基线 10% 无异**。

原因是结构性的：**两者测的都是"与厚层观测是否一致"，而一个在层厚方向平均正确的幻觉
按定义满足观测**。能被数据一致性抓住的错误，采样器本来就不会犯。这条路封死。

### 12.4 采样离散度：成立，AUC 0.919

同一输入采 6 次，取逐体素标准差。它问的是后验的稳定性，不是与观测的一致性，
因此不受 §12.3 那个论证的限制。

灶内平均离散度：虚构 103.4 HU vs 真实 56.0 HU（**1.85×**）。

| 按离散度取最高 | 捕获虚构灶 | 随机 |
|---|---|---|
| 1% | 24.3% | 1% |
| 5% | 59.5% | 5% |
| **10%** | **78.4%** | 10% |
| 20% | 91.9% | 20% |

排序富集 **AUC = 0.919**。**确定性方法原理上给不出这个信号**——它只有一个输出。

### 12.5 换算成检出指标：只够打平，不要宣称更优

把离散度当成"复核提示"，给两个模型夹住真值（thr −400，IoU ≥ 0.3）：

| 标记最高 | DISCARD（全丢） | ORACLE（完美分辨） |
|---|---|---|
| 0% | 72.8 / 79.1 | 72.8 / 79.1 |
| 10% | 79.5 / 77.8 | 79.8 / 79.1 |
| 20% | 85.4 / **74.3** | 86.2 / 79.1 |
| CTHNet 单独 | **79.9 / 78.8** | 79.9 / 78.8 |

复核 10% 只是**接近而非超过** CTHNet；复核 20% 是拿召回换精度。
且 ORACLE 需要医生能分辨真假，而那正是没有 1mm 时做不到的。

**因此不要写"我们的检出优于 SOTA"。** 可写的是：我们的方法能定位自身的不确定区域，
而确定性方法不能——这是独立于精度的价值，但不会体现在 P/R 表里。

### 12.6 按难度门控 vs 按后果门控

| 门控方式 | PSNR | 肺PSNR | LAA950 | CCC |
|---|---|---|---|---|
| 不门控 | 30.92 | 30.67 | 0.16 | **0.787** |
| 按**难度**（可观测 z 梯度 top 20%） | **33.51** | **31.63** | 0.33 | 0.377 |
| 按**后果**（肺实质 < −700 HU） | 31.09 | 31.04 | 0.16 | 0.787 |

难度与临床价值**反相关**：肺静脉 MAE 128 HU 却几乎不影响 LAA；肺实质 MAE 44 HU 却决定全部 LAA。
按难度门控恰好门掉要紧的东西——PSNR 涨 2.6 dB 而 CCC 腰斩。这也顺带说明：
**只要愿意放弃密度学，图像质量随时可以拿满**，这是本文那个权衡最直接的演示。

### 12.7 逐结构重建难度（4 例）

| 结构 | 上采样 MAE | CTHNet MAE | 学习净收益 | 薄层 z 梯度 |
|---|---|---|---|---|
| 肺静脉 | 128.4 | 56.4 | 56% | 129.2 |
| 肺动脉 | 123.4 | 55.3 | 55% | 121.7 |
| 气道 | 97.6 | 51.3 | 47% | 72.5 |
| 肺实质 | 44.4 | 28.3 | 36% | 43.1 |
| 心脏 | 26.0 | 22.6 | 13% | 32.7 |
| 主动脉 | 18.4 | 17.4 | 5% | 21.7 |

**MAE 几乎等于 z 梯度**。难的不是"血管"这个身份，而是它在层厚方向变化快——
主动脉同为血管但粗且走向平直，是全场最好建的。所以解剖知识库对**预测难度**是冗余的；
它不可替代的用途是判断**误差的临床后果**。
