# 阅片研究方案

目标：**重建的 1 mm 相比原始 5 mm，是否让医生更容易判断病灶？以及重建是否会造出不存在的病灶？**

第二问和第一问同等重要。乡镇医院场景下没有薄层可对照，一个凭空造出的结节会变成一次不必要的
转诊或穿刺，而医生没有办法自己发现它是假的。

---

## 1. 必须是三臂，不能是两臂

| 臂 | 作用 |
|---|---|
| A. 原始 5 mm | 下界 = 现状 |
| B. **重建 1 mm** | 待评 |
| C. **真实 1 mm** | 上界，同时是**判定 B 中所见是真是假的唯一依据** |

**只有 A、B 两臂时，这个研究回答不了核心问题。** 医生在 B 中报告一个结节，你无法区分
"5 mm 漏了"和"重建造的"——而这两种情况的临床含义完全相反。C 臂是判据，不是锦上添花。

同一批患者、三臂配对，随机化呈现顺序，医生对臂别盲法。

---

## 2. 病例构成

| 类别 | 建议例数 | 作用 |
|---|---|---|
| 含结节，**直径 < 6 mm** | ≥ 40 | 主要靶点：公开数据实测厚层在此档漏 31% |
| 含结节，6–10 mm | ≥ 30 | 中间档 |
| 含结节，≥ 10 mm | ≥ 20 | 厚层已能检出，作阴性对照（重建不应更差） |
| **确证无结节** | ≥ 40 | **测假阳性的唯一途径** |

无结节组不能省。没有它，"重建造不造假病灶"这个问题在统计上无法回答——所有阳性发现都会被
归因于"5 mm 漏了"。

**结节真值**：以 C 臂（真实 1 mm）为准，由不参与阅片的两名放射科医师独立标注、分歧共识解决，
并报告标注者间一致性（CLAIM 第 17 条）。

---

## 3. 医生任务与记录项

每例每臂记录：

1. **是否发现结节**（是/否）
2. 若是：**位置**（层号 + 肺叶）、**最大径**、**性质**（实性/部分实性/磨玻璃）
3. **置信度**（1–5 分李克特）
4. **图像质量主观评分**（1–5：噪声、边缘锐利度、伪影）
5. 若怀疑图像有**人工痕迹**，单独标记（这一项直接对应幻觉）

同一名医生阅同一患者的不同臂之间**间隔 ≥ 2 周**（洗脱期），避免记忆效应。

至少 **3 名**放射科医师，报告医师间一致性。

---

## 4. 主要与次要终点

**主要终点**：< 6 mm 结节的**检出灵敏度**，B 臂 vs A 臂（配对）。

**次要终点**：

- 无结节病例的**假阳性率**，B 臂 vs A 臂 —— 安全性主终点
- B 臂 vs C 臂的灵敏度差（重建离真薄层还有多远）
- 置信度与主观图像质量评分
- **JAFROC / FROC** 分析（同时利用位置与置信度，比单纯灵敏度更充分）
- 阅片时间

---

## 5. 与自动指标的对照

阅片结果出来后，必须回头核对自动指标是否预测了它。这是让自动指标可信的唯一途径：

- **采样离散度**（`--samples 6 --save_std`）标出的可疑区域，是否与医生标记的"人工痕迹"重合？公开数据上排序富集 AUC = 0.919，需在真人上验证
- **逐例证书**判为 flag/abstain 的病例，医生评分是否更差？
- 自动"对比度保留"是否与医生检出相关？

若离散度在真人研究中同样有效，它就能作为**部署时的自动提示**——这是确定性方法原理上给不出的东西。

---

## 6. 素材导出（上机时一并做，回来补不了）

```bash
python scripts/export_reader_study.py --root PRIVATE/henan/pairs \
    --recon RECON/henan --out READER --n 130
```

导出内容：

- 三臂 NIfTI，文件名**已随机化并打乱臂别**（对照表留在院内，不出院）
- `reader_manifest.csv`：随机 ID → 臂别 → 患者 sha1（**不出院**）
- `reader_blank.csv`：医生填写用的空表

**注意**：阅片素材是图像，**不能带出医院**。阅片须在院内完成，或按院方数据管理规定另行审批。
带出来的只有填好的评分表（不含图像、不含患者标识）。
