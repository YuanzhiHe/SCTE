# 河北队列最终结果 — 复核报告

对 `result conclusion.docx`（2026-09-17）的逐节复核。分三部分：**站得住的结果**、
**必须更正的结论**、**下一步**。

---

## 一、站得住的结果（可以写进论文）

### 1.1 主结果：密度学终点大幅优于两条基线

n=444，外部队列，两家厂商。

| | LAA950 bias | LAA950 MAE | LAA950 CCC | P15 bias |
|---|---|---|---|---|
| Lanczos | −4.41 pp | 4.46 | 0.689 | +23.46 HU |
| CTHNet | −4.48 pp | 4.54 | 0.682 | +19.34 HU |
| **ZS-Ours** | **−0.26 pp** | **2.17** | **0.933** | **+2.53 HU** |
| **Ours-local** | +1.04 pp | 2.26 | **0.948** | −3.99 HU |

误差减半、偏差降一个量级，配对 Wilcoxon p < 10⁻¹⁴。这是标题结果。

**同时成立的一条更强的话**：CTHNet 比 ZS-Ours 高 **3.96 dB** PSNR，LAA950 的 MAE
却是两倍多（4.54 vs 2.17）。图像保真度与密度学准确性在本任务上解耦——这是本文的
核心论点，而且现在有 444 例外部数据支撑。

### 1.2 位移机制在独立扫描仪上复现

`Public zero-shot` δ̂ = **−16.32 ± 0.65 HU**，444 例中 sd 仅 0.65 —— 一个常数位移，
不是随机误差。这与公开数据上的 19–21 HU 同量级、同性质。跨域成立。

`Bias-only` 臂 δ̂ = −39.8 ~ −41.8 HU，认证率 0/444，佐证单纯的标量改正不是解。

### 1.3 五叶与分层分析

五叶 CCC 均 > 0.92（0.929–0.956），支持区域性评估。四分位分层显示轻度回归向均值
（Q1 +1.31 pp / Q4 −1.27 pp），是可以如实报告的性质。

### 1.4 积分步数不敏感

steps 32 vs 64，LAA 各项差异 < 0.2 pp。可作稳健性说明。

---

## 二、必须更正的结论

### 2.1 §6.3「ZS-Ours 认证率 0.0%」— 数字取错了行

§2.1 里有两条不同的臂：

- `Public zero-shot`（= `public.pt`，确定性回归）→ **0/444**
- `Flow zero-shot`（= `public_flow.pt`，即 ZS-Ours）→ **56/444 = 12.6%**

`volume_zs_ours.csv` 对应的是后者。§6.3 取了前者的 0%，于是「本地微调 98.2% vs
零样本 0%」这个对比被放大成了不存在的量级差。真实对比是 **98.2% vs 12.6%**。

### 2.2 §2.2「本地微调后结构保真度优于 thin-slice GT」— 不可能成立

Oracle 的"重建"就是 1mm 真值本身，是构造上的天花板。一个模型在同一套阈值下比真值
更容易通过，只能说明**这个判据在该模型上失效了**，不能说明模型比真值好。

原因在 `scte_r/agentic.py:238`：

```python
R = op(x_hat) - y_thick
rho_struct = (R - delta).norm() / denom
```

ρ_struct 只看 `A(x̂)`，即前向算子的**值域**，对零空间结构性失明。真值对自己的厚层
观测也有残差（采集噪声、运动、真实 SSP 与拟合算子的偏差）；本地 flow 在本队列上
训练过，它比真实物理更严格地满足我们拟合的那个 `A`。δ̂ 的 sd 0.51 vs Oracle 的 2.45
是同一件事的第二个签名。

**同一张表里的 Landweber 行是决定性证据**：δ̂ = 0.00 ± 0.00、ρ_struct = 0.000 ——
完美满足前两项，而 PSNR 只有 27.62。Landweber 迭代到数据一致，所以这两项按构造归零。
一个重建质量平平的方法在两项上拿满分，正是"数据一致性无法判别零空间内容"的直接演示。

**正确表述**：证书的三项无参考量在**域内饱和**。公开 flow 被 ρ_struct 拒掉 76%，
说明这一项本来有判别力；本地训练后判别力消失。因此本地臂的质量**必须由有参考的指标
确立**（§3 的整卷 LAA/PSNR），不能由证书确立。98.2% 不是质量证据。

**这对部署反而是好消息**：证书的有效域正好是部署场景。乡镇医院没有 1mm 设备，也就
不可能有本地配对数据，所以他们用的永远是零样本臂——证书在那里有判别力。这是一条
有边界的结论，比"通过率 98%"有意思得多，也更站得住。

### 2.3 §5.2「分扫描仪校准导致认证率坍缩」— 比较的是两个不同的模型

三处不一致：

1. 算术：6/317 + 0/127 = **6/444 = 1.4%**，不是「44/444」，也不是「12.6%」
2. 12.6% 是 `cert_flow_zeroshot`（合并标定 + 公开 flow）的数
3. **分机器那两组用的是 `public_flow.pt`**（`per_scanner.py --flow_ckpt` 默认值），
   而 98.2% 用的是 `flow_local.pt`

所以这张表比的是"分机器标定的公开模型"对"合并标定的本地模型"，模型和标定同时变了。
由此得出"全局校准为权威配置"没有依据——差异可能全部来自模型。

想支持这个结论，需要的是**同一个模型**在两种标定下的对照（`per_scanner.py
--pooled_control`）。在跑之前，§5.2 的结论应当撤下。

另注：两台机器的有效宽度早期日志记为 2.50 mm，本报告记为 2.75 mm。同样的代码和输入
不该给出两个值，需确认哪份是当前的。

### 2.4 §3.7 的方向标反了

| LAA910 | Ours-local | ZS-Ours |
|---|---|---|
| bias | +1.72 pp | **−0.32 pp** |
| MAE | 2.52 | **1.91** |
| CCC | 0.978 | **0.986** |
| LoA 宽度 | 9.60 | **9.07** |

ZS 在**每一项**汇总统计上都更好。那么 p = 1.69×10⁻⁹ 的配对检验只可能**支持 ZS**，
不可能支持 Ours-local。结论列写的「Ours 在 LAA910 显著优」与自己的数据矛盾。

LAA950 的 p = 1.96×10⁻² 同样没写方向，而 MAE 是 ZS 更低（2.17 vs 2.26）。两项都需要
把差值的符号和置信区间打出来，不能只报 p 值。

### 2.5 §3.3 隐去了 P15/P10 的 MAE

只给了 bias。加上 MAE 之后（n=148 共同子集）：

| | P15 MAE | P10 MAE |
|---|---|---|
| ZS-Ours | 7.80 | 10.07 |
| Ours-local | **9.54 (+22%)** | **12.37 (+23%)** |

这是所有终点里**最大的单项变化，方向是变差**。而 Perc15 在 COPD 定量中通常比
LAA-950 更受推荐（不依赖硬阈值，对重建核和剂量更稳健）。把它做坏 22% 换 LAA950 的
CCC 从 0.933 到 0.948，这笔账要如实呈现，不能省略。

### 2.6 §3.4「nan 是 flow 类分支的固有属性」— 不是

非有限值只出现在特定病例上，其余 442 例正常。已确认那两例的**肺掩膜是好的**、不在
abstain 名单里 —— 是模型自己发散了。

这条曾经是个安全漏洞：NaN 与任何阈值比较都返回 False → 不产生违规 → 判为
**certified**；而 `LAA950` 把 `NaN < −950` 当 False，那些体素计入分母当正常肺实质，
于是算出一个**有限但错误**的 LAA，还带着认证。已修（commit 19cdb66，改为
`abstain / non_finite_reconstruction`），并已审计确认磁盘上的三个 cert CSV 中
**没有病例靠这个漏洞进入 certified**，436/444 不需要扣减。

但"固有属性"这个说法要去掉——它是一个已修复的缺陷，而且值得在论文的局限性里写明。

### 2.7 §6.2「MAE 2.26 pp → 临床可接受」— 应当用 LoA 论证

MAE 是平均值；决定单次扫描能否被信任的是 **95% LoA = [−3.18, +5.26] pp**。在 10%
的诊断临界值附近，上限意味着 10% 可被报成 15.3%，**跨过 15% 这条线**。

§3.6 的 Q1 层更直接：ref 1.00% → rec 2.31%，**相对高估 131%**。对"准确检出且不要
误诊"这个终点，低负荷段的正偏就是假阳性方向，会转化成不必要的转诊。

正确写法是报 LoA 并明确说明：本方法在群体层面近似无偏，个例层面的 95% 一致性区间为
±X pp，因此适用于筛查分诊而非单例确诊阈值判定。

### 2.8 §7 「困难案例」定性偏轻

`0db96bf6ffb3`：PSNR **−1.03 dB**、lung PSNR **−6.20 dB**。负 PSNR 意味着误差能量
超过信号能量 —— 这是失败，不是"困难"。它的 LAA950 报了 15.1%（真值 11.0%），正好
落在 15% 临界值上。

**必须查这一例的判决**：如果它被 certified，那就是一个"证书为失败重建背书"的实例，
是审稿人一定会问的安全问题，也必须写进论文。

---

## 三、重新解读：本地微调不是赢家

报告的框架是"本地微调 = 最优配置"。数据不支持这个框架。

| | ZS-Ours | Ours-local |
|---|---|---|
| LAA950 bias | **−0.26** | +1.04 |
| LAA950 MAE | **2.17** | 2.26 |
| LAA950 CCC | 0.933 | **0.948** |
| LAA950 LoA 宽度 | 10.69 | **8.44** |
| LAA910（全部四项） | **全胜** | — |
| P15/P10 MAE | **全胜（好 22%）** | — |
| PSNR | 26.81 | **28.34** |
| 认证率 | 12.6% | 98.2%（已饱和，无判别力） |

**本地微调换来的是 PSNR +1.5 dB 和 LAA950 的一致性收窄，代价是所有百分位指标和
LAA910 变差、以及证书失效。**这不是"更优"，是一次有得有失的权衡。

而且它**不可部署**：乡镇医院没有 1mm 设备，就没有本地配对数据可微调。所以它在论文
里的位置是"若有本地配对数据时的上界"这条消融，不是主臂。

**主臂是 ZS-Ours**，而且它足够强：LAA950 偏差 −0.26 pp、MAE 2.17，对比 CTHNet 4.54、
Lanczos 4.46。这才是与部署场景一致的那条臂。

### 混合臂：假设被否定

`ZS-Hybrid`（公开 flow + CTHNet base）PSNR **25.92**、SSIM **0.715** —— 是全部六条
臂里**最低**的。它原本是唯一可能同时拿到 CTHNet 的 PSNR 和 Ours 的密度学的候选，
现在可以确定它做不到。

原因已定位：`public_flow.pt` 训练时**没有** `--base_recon`（见 `scripts/run_hybrid.sh:8`，
混合臂本应使用自己的检查点 `runs/HY_flow.pt`）。`scte_r/flow.py:103` 中 `y_up` 不仅
被加回，还**条件化速度场**——换掉 base 等于给速度网络喂分布外输入。δ̂ 从纯 flow 的
−0.73/−1.45 变成 −2.50/−3.65，是同一原因的独立佐证。

**`scripts/run_private.sh:277` 是同样的错配组合，是管线里的 bug，应当修掉或删除。**

因此"图像质量非劣于 SOTA"这条目标**没有达成**，三条臂距 CTHNet 分别为 −3.96 /
−2.43 / −4.85 dB。建议不再追求非劣，改用定量的取舍表述：

> 相对冻结的 CTHNet 骨干，流残差以 2.43 dB 的 PSNR 换取 LAA-950 偏差缩小 4.3 倍
> （−4.48 → +1.04 pp）。图像保真度与密度学准确性在本任务上不是同一个量。

代价的来源是可计算的：`s_r = 0.028`、`u ~ N(0,1)`，流加上去的随机纹理标准差约
28 HU —— 正是这层纹理在花 PSNR，也正是它让密度分布不被磨平。

---

## 四、下一步（按优先级）

| # | 动作 | 成本 | 为什么 |
|---|---|---|---|
| 1 | 查 `0db96bf6ffb3` 的 verdict | 秒级 | 若为 certified，是必须披露的安全问题 |
| 2 | 打印 §3.7 各配对差的**符号与 95% CI** | 秒级 | 现结论列方向存疑 |
| 3 | `common_subset.py` 把 ZS-Hybrid（n=150）纳入同一子集 | 秒级 | 现在 150 vs 444 不可比 |
| 4 | `per_scanner.py --pooled_control` | ~1 h GPU | §5.2 的结论唯一的支撑 |
| 5 | 确认有效宽度是 2.50 还是 2.75 mm | 分钟级 | 两份日志互相矛盾 |
| 6 | 修 `run_private.sh:277` 的错配 | 分钟级 | 管线 bug |

第 1、2 条决定论文能不能定稿；第 4 条决定 §5.2 留不留。

---

## 五、可以定下来的主张

1. **主张一（机制）**：厚→薄重建中，确定性回归的最优解是一个约 16 HU 的全局 HU
   位移，与损失配方无关。跨两个独立队列、两家厂商成立（公开数据 19–21 HU，河北
   −16.32 ± 0.65 HU）。

2. **主张二（解耦）**：已发表的厚→薄方法显著提升 PSNR 却不改善密度学一致性。
   CTHNet 比 ZS-Ours 高 3.96 dB，LAA950 的 MAE 是其 2.1 倍、偏差是其 17 倍。

3. **主张三（证书）**：证书是**站点级部署闸门**，不是逐例保证。

   *站点级有效*（2026-09-18 翻转审计确立）。四个「模型×域」组合，认证率干净分离：

   | 域 | 模型 | 标定 | 认证率 |
   |---|---|---|---|
   | 公开 RPLHR | 公开（域内） | 公开 | 40/50 = **80%** |
   | 河北合并 | 本地（域内） | 河北 | 436/444 = **98.2%** |
   | 河北 SIEMENS（软核 I50f） | 公开（同核族） | 河北 | 50/311 = **16.1%** |
   | 河北 UIH（锐核 B_SHARP_C） | 公开（未训练过） | 河北 | 6/126 = **4.8%** |

   标定在三个组合里都是本地拟的，变的只有模型对该域是否胜任，**域内 80–98% 对
   外域 5–16%，无重叠**。关键在于 **UIH 本来就在标定域内**（Henan CAL 50 里有
   18 例同核 UIH），公开模型仍被标记 94% —— 所以证书判的不是"协议没见过这台
   机器"，而是"模型没学过这类数据"。这正是部署闸门该判的东西：标定扫描仪修不好
   模型的无能。

   **但闸门的工作点很窄，这一条必须写进局限性。**按站点看标记率：SIEMENS
   261/311 = **83.9%**（终点是好的，flip@10 仅 1.6%），UIH 120/126 = **95.2%**
   （终点确实崩，flip@10 27.6%）。要把"能部署"和"不能部署"分开，阈值得落在
   83.9% 与 95.2% 之间 —— 间隔太窄，而且只有两个站点，无法确定该阈值。更准确的
   说法是：**连续的项值（\|s\| 合并 AUC 0.75–0.85）比二值判决携带更多域信息**，
   当前阈值把两个外部域都推到了"几乎全标记"，因为 τ 描述的是完美重建的零分布，
   不是可接受重建的分布。

   *逐例无效*。同一域内判决不携带任何信息：SIEMENS 组认证 0/50 翻转、标记 5/261
   翻转，Fisher **p = 1.0**。合并后的 p = 0.005 完全来自域构成——证书标记了
   120/127 例 UIH，而 UIH 恰是终点崩掉的域。三项与逐例 |误差| 的相关在域内消失
   或反号（δ −0.05、ρ_struct +0.13、s −0.12）。合并 AUC（\|s\| 0.75–0.85、
   \|δ\| 0.68–0.74）是**域对比**，不是逐例误差追踪；论文里必须同时给出域内 AUC，
   否则读者会把它当成可用的逐例检测器。

   另有一条设计发现值得单列：**ρ_struct 的 AUC 区间 0.42–0.56 跨过 0.5，是零
   信号，而它恰是否决病例最多的一项**（合并 flow 中单独否决 76.4%）。干活最多的
   判据没有判别力。

   因此 `certify_local.py` 那种扩大覆盖的做法救不回逐例认证——**缺口在判据，不在
   覆盖**。

三条都有 444 例外部数据支撑。第三条从"通过率高"改成"站点级闸门"之后，既诚实又
可部署：它回答的是"这个模型能不能用在这家医院"，而那正是乡镇部署要问的第一个
问题，也是唯一能在没有 1mm 参考的现场回答的问题。
