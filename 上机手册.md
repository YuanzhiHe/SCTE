# 上机手册（单文件版）

带这一份就够。所有内容自包含，不需要翻其它文档。

**对象**：河北大学附属医院 203 例（内部训练）+ 河南省人民医院 491 例（外部验证），均 1 mm + 5 mm。
**前提**：设备在第三方手里、接触时间有限。这份文件的目标是**当天不写代码、不调试**。

---

# 第 0 页：一页速查

```bash
# 1. 装（约 20 分钟）
git clone git@github.com:YuanzhiHe/SCTE.git && cd SCTE
pip install -r requirements.txt
pip install TotalSegmentator einops timm scikit-image
bash scripts/fetch_baselines.sh

# 2. 自检（约 3 分钟）—— 必须看到 PRE-FLIGHT PASSED
python scripts/preflight.py

# 3. dry run（约 15 分钟）—— 必须过
bash scripts/run_private.sh dryrun /data/hebei hebei

# 4. 正式跑（约 12–18 小时，可分两天，随时 Ctrl-C 续跑）
EXT_CAL=50 bash scripts/run_private.sh full /data/hebei hebei /data/henan
```

**三个硬门，任一不过就停下，不要往下跑**：

| 门 | 通过标志 | 不过怎么办 |
|---|---|---|
| 自检 | `PRE-FLIGHT PASSED` | 看它指出的失败项，多半是 torch/torchvision 通道不一致 |
| dry run | 跑到 `[11/11]` 且无 `!!` | 看 `PRIVATE/hebei/results/01_pairing.log` |
| **对齐自检** | `alignment check PASSED` | **绝对不要继续**，见第 3 节 |

**离院前必须收集的（回来补不到）**：伦理批件号、人口学表、结节标注、协议元数据、阅片素材。见第 6 节。

---

# 1. 安装

## 1.1 依赖

```bash
git clone git@github.com:YuanzhiHe/SCTE.git && cd SCTE
pip install -r requirements.txt
pip install TotalSegmentator einops timm scikit-image
bash scripts/fetch_baselines.sh     # I3Net 按许可证要求用时拉取，不随仓库分发
```

**torch 和 torchvision 必须来自同一个 CUDA 通道**：

```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
```

不一致时 `import torchvision` **是成功的**，只会在 TotalSegmentator 内部炸在
`torchvision::nms does not exist`——那时你已经在医院了。第 2 节的自检会强制解析这个算子，当场抓出来。

## 1.2 TotalSegmentator 权重（320 MB）

第一次运行会联网下载。医院能上网就不用管；不确定就在自己机器上先跑一次再带过去：

```bash
tar czf ts_weights.tgz -C ~ .totalsegmentator     # 自己机器
tar xzf ts_weights.tgz -C ~                        # 医院机器
```

**没有它，LAA 的分母会退化成 HU 窗，那不是临床 LAA-950**——同一例会从 0.34–1.57% 被抬到 9.9–13.6%。
这是本项目犯过并更正的一个错。

## 1.3 完全没有外网的机器

在有网的机器上跑 `bash scripts/make_bundle.sh`，得到含代码、权重、TS 权重、离线 wheels 的自包含包，
拷 U 盘带走，按包内 `INSTALL.txt` 安装。

---

# 2. 自检（必做）

```bash
python scripts/preflight.py
```

它不是 import 检查。它会**造一对已知亚 slab 偏移的合成 CT，跑真正的阶段 1 代码，验证偏移被精确还原**，
然后跑算子拟合和证书。除 DICOM 读取本身外，上机要用的每一环都验到了。

期望输出：

```
torch + CUDA                    OK
torchvision matches torch       OK   nms resolves
TotalSegmentator + weights      OK
build synthetic pairs           OK   true offset slab+1 sub+3
prep_pairs recovers the offset  OK   4/4 paired, 0 skipped
alignment self-check passes     OK
protocol operator fit           OK
certificate on perfect recon    OK

PRE-FLIGHT PASSED
```

**没看到最后那行就不要往下走。**

---

# 3. 数据格式与 dry run

## 3.1 目录要求

一个病例一个子目录，里面放 1 mm 和 5 mm 两个序列（DICOM 文件夹或 NIfTI 均可）：

```
/data/hebei/
  CASE001/  1mm/*.dcm   5mm/*.dcm
  CASE002/  thin.nii.gz thick.nii.gz
```

薄/厚由 z 间距自动判定，读不到就按层数；脚本已默认带 `--thin_hint 1mm --thick_hint 5mm` 强制。

## 3.2 dry run

```bash
bash scripts/run_private.sh dryrun /data/hebei hebei
```

6 例、3 epoch，**只验管路，不要解读它的任何数字**。它专门暴露自检验不到的那一环：
DICOM 读不出、薄厚判反、HU 不是 HU、序列不是同一次采集。

数据是归一化值而非 HU 时（HU 范围显示成 `[0, 4095]` 之类）：

```bash
RESCALE="3072 -1024" bash scripts/run_private.sh dryrun /data/hebei hebei
```

## 3.3 对齐自检 —— 这一项单独讲

阶段 1 之后会自动抽 20 例检查**亚 slab z 错位**，失败直接 `exit 1`。

为什么单独讲：**亚 slab 错位在所有日志里都看不见**。它会把插值基线压低约 3 dB，
并悄悄错移整个证书赖以成立的前向算子。本项目在公开数据上因为它整体重跑过一次
（150 例一致选择 thick+1/thin+3，前向算子残差降 55%）。

看到 `ALIGNMENT CHECK FAILED` 时：**停下**。先查 `--thin_hint/--thick_hint` 是否判反、HU 范围是否正确，
不要试图绕过。

---

# 4. 正式跑

```bash
EXT_CAL=50 bash scripts/run_private.sh full /data/hebei hebei /data/henan
```

- 第 2 个参数 = **内部训练队列**（河北）
- 第 4 个参数 = **外部验证队列**（河南）
- `EXT_CAL=50`：从河南划 50 例做**扫描仪物理标定**（有效层厚、证书阈值），其余 441 例只上报。
  去掉它就是最严格的外部验证（河北的常数直接套到河南，河南一个数都不拟合）
- **每阶段断点续跑**：Ctrl-C 后重跑同一命令自动跳过已完成阶段，`FORCE=1` 强制重做

## 4.1 十一个阶段

| # | 内容 | 拟合/评测 | 耗时 |
|---|---|---|---|
| 1 | 配对 + z 对齐（薄层分辨率）+ 匿名化 + **对齐自检** | — | 20–30 min |
| 2 | TotalSegmentator 肺叶掩膜 | — | 40–60 min |
| 3 | TRAIN/TEST（+ 标定）划分 | — | 秒级 |
| 4 | 该队列的 protocol 常数 | 标定集 | 5 min |
| 5 | 学该扫描仪的前向算子 `A_θ` | 标定集 | 10 min |
| 6 | 完美重建零分布 → 证书阈值 | 标定集→TEST | 10 min |
| 7 | **头条**：公开权重零样本 δ̂ + Landweber / 仅偏置对照 | TEST | 25 min |
| 8 | 骨干：本队列训 CTHNet，缓存重建后冻结 | TRAIN | 2–3 h |
| 9 | 生成级：冻结骨干上训流匹配残差 | TRAIN | 1 h |
| 10 | 证书 + 确定性回归对照 | TRAIN→TEST | 1 h |
| 11 | 整卷：同一模型两个输出 + Lanczos / CTHNet 对照 | TEST | **6–10 h** |

阶段 11 的耗时按**面内尺寸平方**增长。上表按 512×512 估（你们的数据）。实测：512×512
单采样约 6 min/例，4 采样约 24 min/例。

跑完自动写出 `PRIVATE/<tag>/results/SUMMARY.txt`。

## 4.2 时间不够时的取舍

**阶段 1→7 必须做**——最重要的那个数（公开权重在他们设备上的 δ̂）就在阶段 7，而且**不需要训练**。
即使当天只有两小时，这个数也能拿到。

阶段 8–9 可砍预算：`BSTEPS=8000`、`--epochs 100`。阶段 11 可 `--n 15`，4 采样那一路事后补。

## 4.3 划分纪律（不要破坏）

凡是**拟合**出来的（protocol 常数、前向算子、证书阈值、骨干、流匹配）只用河北或标定集；
凡是要**上报**的只在河南测试集上测。脚本已经这么写了，静态审计过，不要手工改 `--root`。

---

# 5. 要拿到的数

## 5.1 四个核心数

1. **公开权重在他们设备上的 δ̂**（阶段 7）。公开数据上是 −15.4 HU。
   若这里也是十几 HU 的负偏移，就证明"平移换指标"不是某台设备的偶然。
2. **确定性回归的 δ̂**（阶段 10）。公开数据上所有确定性臂都落在 19–21 HU，**与损失配方无关**
   （生物标志物损失权重砍三分之二，δ̂ 从 19.08 变到 19.09）。在真实临床设备上复现这一点，
   是本文机制主张最硬的一块。
3. **看图输出 vs 骨干的肺 PSNR 差距**（阶段 11）。公开数据上 −0.25 dB。
4. **指标输出的密度学优势**（阶段 11）。公开数据上 LAA-950 好 1.5×、LAA-910 好 2.2×、CCC 好 3.9×。

## 5.2 公开数据上的对照值（用来判断私有数据是否异常）

50 例整卷，TS 肺叶掩膜：

| 臂 | 肺PSNR | LAA950 MAE | LAA950 CCC (95% CI) |
|---|---|---|---|
| Lanczos-3 | 27.65 | 0.78 | 0.259 [0.197, 0.333] |
| TVSRN | 31.08 | 0.90 | 0.068 [0.028, 0.135] |
| CTHNet | 31.60 | 0.82 | 0.152 [0.094, 0.244] |
| I3Net | 31.34 | 0.86 | 0.100 [0.050, 0.185] |
| **ours（单采样）** | 30.28 | **0.54** | **0.594 [0.501, 0.674]** |

证书：ours 的 \|δ̂\| = 0.32 HU、47/50 通过；确定性臂 19–21 HU、0/50。

**如果私有数据上确定性臂的 δ̂ 不在十几到二十几 HU，先怀疑数据准备而不是结论。**

## 5.3 一个口径陷阱

河南队列病更重、更离散，**所有臂的 CCC 都会跳上去**。CCC 衡量的是"能不能区分病人"，
不是"数准不准"——本项目实测：误差最小的轻度层 CCC 最低（0.519），误差最大的重度层反而更高（0.532）。

**不能拿 CCC 当进步的证据，必须同时报 MAE 和 bias（以及 Bland-Altman 一致性界限）。**

---

# 6. 院内必须收集的（离院补不到）

这一节是这份手册里最容易被忽略、代价最大的部分。

| 项 | 怎么拿 | 为什么补不到 |
|---|---|---|
| **伦理批件号、知情同意方式** | 向院方索取 | 论文必填（CLAIM 第 7 条） |
| **人口学与临床特征** | 院内统计：年龄、性别、吸烟史、肺功能 | 数据不出院 |
| **协议元数据** | 自动：`prep_pairs.py --protocol_meta` 已默认开启，写入 `pair_audit.csv` | DICOM 头不出院 |
| **结节/病灶标注** | 需院内放射科医师在**真实 1 mm** 上标注 | 影像不出院 |
| **阅片研究素材** | 见 6.1 | 影像不出院 |
| **数据流图所需计数** | 记录：总例数、各排除原因的例数 | 事后重建不了 |

协议元数据只导设备参数（厂商、型号、重建核、kVp、mAs、标称层厚、滤波、软件版本），
**白名单机制**，病人姓名/ID/机构名不会被导出（已用带 PHI 的 DICOM 验证过）。

## 6.1 阅片研究素材（若要做）

```bash
python scripts/export_reader_study.py --root PRIVATE/henan/pairs/test \
    --recon RECON/henan --out READER --n 130
```

**必须三臂**：原始 5 mm（下界）/ 重建 1 mm（待评）/ **真实 1 mm（上界兼判据）**。

只有前两臂时，医生在重建里报一个结节，你**无法区分"5 mm 漏了"和"重建造的"**——
而这两种情况临床含义完全相反。真实 1 mm 那一臂是判据，不是锦上添花。

**必须混入确证无结节的病例**（建议 ≥ 40 例）。没有它，"重建造不造假病灶"在统计上无法回答。

导出物：文件名是哈希、臂别不可见、厚层臂也重采样到 1 mm 网格（否则数层数就知道是哪一臂）。
`reader_manifest_DO_NOT_EXPORT.csv` 留院内，只有填好的评分表能带出。

**阅片素材是图像，不能带出医院**，须在院内完成或另行审批。

---

# 7. 能带走什么

只带 `PRIVATE/<tag>/results/` 下的 `*.csv`、`*.log`、`SUMMARY.txt`：
**逐例数字，不含图像、不含病人标识**（case 列是 sha1 前 12 位）。

`PRIVATE/`、`ID_MAP_DO_NOT_EXPORT.csv`、所有 `*_thin/_thick/_lung/_base.npy` 都在 `.gitignore` 里，
即使在医院机器上 `git add -A` 也不会被提交。要推结果回来必须显式 `git add -f`——
这个摩擦是故意的，推之前自己看一眼。

---

# 8. 现场排障

| 症状 | 原因 | 处理 |
|---|---|---|
| 自检在 torchvision 那项失败 | torch/torchvision CUDA 通道不一致 | 用同一 `--index-url` 重装两者 |
| `no readable DICOM series` | 病例目录下不是 DICOM 或层级太深 | 把序列目录放到病例目录下一层 |
| 全部 `[skip] ... not CT-like` | 数据是归一化值不是 HU | 加 `RESCALE="3072 -1024"`（先用一例确认空气峰落在 −1000） |
| **`ALIGNMENT CHECK FAILED`** | 阶段 1 没把配对落到算子网格上 | **停下**，查 hint 与 HU 范围，不要绕过 |
| 大量 `off=±4` | 两个序列不是同一次扫描 | 排除这些例；这本身是可报告的发现 |
| 阶段 4 报 `only N usable case(s) ... need at least 3` | 队列（的一半）太小 | 报错会区分"单位不对"和"例数不够"；后者用 `NOSPLIT=1` |
| 阶段 8 显存不足 | CTHNet 在 256 裁剪下很重 | 已默认 `--amp --grad_ckpt`；**权重与裁剪尺寸绑定，减 crop 需重训** |
| 阶段 11 特别慢 | 整卷 × 64 步 × 两个输出 | `--n 15`；4 采样那一路最后补 |
| TS 卡在下载 | 权重没带 | `cp -r ts_weights ~/.totalsegmentator`（1.2 节） |
| 没有 `_lung.npy` 但流程继续了 | TS 失败 | 所有 LAA 都是 HU 窗口径，**不能当临床值报** |
| 想中途停下 | — | Ctrl-C，重跑同一命令自动跳过已完成阶段 |
| 完全没有 GPU | — | 阶段 8 的 CTHNet 在纯 CPU 上不可行，只能跳过骨干退回纯流匹配 |

---

# 9. 方法一页纸（需要向院方解释时用）

**问题**：厚层 CT（5 mm）算出的肺定量指标系统性偏低，小结节可检出性也下降。
公开数据实测：结节对比度只保留 47%，**< 6 mm 的结节有 31% 掉到检出阈值以下**，
而 ≥ 10 mm 完全不受影响。

**已有方法的问题**：三个已发表的超分方法（TVSRN / CTHNet / I3Net）能把图像质量提高
3.4–4.0 dB，但**肺气肿定量一致性一个都没改善，甚至低于三次插值**。原因是它们通过一个
约 20 HU 的全局 HU 平移来"对上"指标——这个平移**医生看不出来**，但让每个下游定量测量都错。

**本方法**：
1. 冻结的强骨干负责图像质量
2. 残差空间的流匹配采样器负责恢复密度分布尾部（训练目标不含任何指标项，结构上没有平移动机）
3. **逐例证书**：只用厚层观测就能估出全局 HU 位移 δ̂，无需 1 mm 参考
4. 同一次积分给两个输出：单采样报指标，4 采样均值供阅片

**已验证**：δ̂ = 0.32 HU（确定性方法 19–21 HU），证书 47/50 通过，
LAA-950 一致性界限从 −2.83~+1.20 pp 收窄到 −1.88~+0.86 pp。

**如实声明的局限**：本方法在病灶检出上**不优于**骨干（LUNA16 94.9% vs 94.9%）；
形态保真度略低于确定性骨干；证书只保证全局密度，不保证局部。

---

# 附录 A：CLAIM 清单（论文撰写用，上机时只需看第 6 节）

对照 CLAIM 2024 条目逐项填写。**"待补"标注的是上机后才能完成的条目**，写在这里是为了
上机时不遗漏——其中若干项（原始 DICOM 元数据、伦理批件号、招募时间窗）一旦离开医院就补不回来。

状态标记：✅ 已具备 · ⏳ 上机后补 · ❌ 尚缺且需决策

---

## 摘要与引言

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 1 | 摘要标明使用 AI 方法 | ⏳ | 撰写时 |
| 2 | 结构化摘要 | ⏳ | |
| 3 | 临床背景与现有做法 | ✅ | 乡镇医院仅有厚层 CT；实验报告 §12 |
| 4 | 研究目标与假设 | ✅ | 假设：厚层系统性损害小结节可检出性与肺定量指标，且现有超分方法只修复前者 |

## 方法 — 研究设计

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 5 | 前瞻/回顾性 | ⏳ | **回顾性**（既有影像） |
| 6 | 研究目标（诊断/预后/…） | ✅ | 图像恢复 + 定量标志物一致性 + 病灶可检出性 |
| 7 | 伦理批件、知情同意 | ⏳ | **批件号必须在院内记录**，离院补不到 |

## 方法 — 数据

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 8 | 数据来源 | ✅/⏳ | 公开：RPLHR-CT、LUNA16。私有：河北 203 / 河南 491 |
| 9 | 纳入排除标准 | ✅ | 需同一次采集的 1mm + 5mm 配对；z 间距 ≤ 1.25mm（LUNA16）；肺重建核 |
| 10 | 数据预处理 | ✅ | `scripts/prep_pairs.py`，含**薄层分辨率 z 对齐**（§11.1） |
| 11 | 去标识化 | ✅ | sha1 前 12 位；`ID_MAP_DO_NOT_EXPORT.csv` 不出院 |
| 12 | 训练/验证/测试划分 | ✅ | 河北训练、河南**外部验证**；`EXT_CAL=50` 仅供物理标定 |
| 13 | 划分层面（患者级） | ✅ | 患者级，无泄漏（静态审计见 §11） |
| 14 | 数据采集参数 | ⏳ | `prep_pairs.py --protocol_meta` 导出设备/采集元数据（白名单，无 PHI） |

## 方法 — 参考标准（真值）

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 15 | 真值定义 | ✅ | 同次采集的真实 1 mm 序列 |
| 16 | 真值标注者与资质 | ✅/⏳ | LUNA16：4 名放射科医生共识；私有队列结节标注**待定** |
| 17 | 标注者间一致性 | ❌ | 阅片研究需报告 |

## 方法 — 模型

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 18 | 模型结构 | ✅ | 冻结骨干（CTHNet，已发表）+ 残差空间流匹配；`scte_r/flow.py` |
| 19 | 训练方法与超参 | ✅ | `scripts/run_private.sh` 阶段 8–9，参数固化在脚本内 |
| 20 | 初始化 / 迁移学习 | ✅ | 骨干在本队列从零训练（标度曲线示 203 例充分，§"骨干标度"） |
| 21 | 集成 | ✅ | 单采样报指标 / 4 采样均值供阅片 —— 同一次积分两个输出 |
| 22 | 软件与版本 | ✅ | `requirements.txt`；`scripts/preflight.py` 校验环境 |

## 方法 — 评估

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 23 | 评价指标及理由 | ✅ | 图像：PSNR/SSIM（全体积与肺区）。定量：LAA-950/910、Perc15/10/5，**Bland-Altman + 一致性界限**。病灶：对比度保留与可检出率 |
| 24 | 统计方法 | ✅ | `scripts/agreement.py`：bootstrap 95% CI（5000 次重抽），逐例配对差异检验 |
| 25 | 与现有方法比较 | ✅ | 四种插值 + **三个已发表方法官方实现**（TVSRN / CTHNet / I3Net），同一划分重训、同一协议评测 |
| 26 | 失效分析 | ✅ | 虚构/漏检审计（`scripts/hallucination.py`）+ 采样离散度定位（AUC 0.919） |

## 结果

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 27 | 数据流图 | ⏳ | 需绘制：筛出多少例、为何排除 |
| 28 | 人口学与临床特征 | ⏳ | 院内统计（年龄、性别、吸烟史、肺功能） |
| 29 | 模型表现 + 置信区间 | ✅ | `results/agreement_*.csv` |
| 30 | 失效案例 | ✅/⏳ | 幻觉审计已具备；需补代表性图例 |

## 讨论

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 31 | 局限 | ✅ | 见下 |
| 32 | 临床意义 | ✅ | <6mm 结节在厚层下 31% 掉到阈值以下，重建补回 70% |

## 其他

| # | 条目 | 状态 | 位置 / 说明 |
|---|---|---|---|
| 33 | 注册 | ❌ | 若含阅片研究，考虑预注册 |
| 34 | 资助与利益冲突 | ⏳ | |
| 35 | 数据/代码可得性 | ✅ | 代码 github.com/YuanzhiHe/SCTE；开发全程在公开数据上可复现 |

---

## 必须写进"局限"的条目

按发现顺序，全部为实测而非推测：

1. **LUNA16 的厚层是模拟的**，非真实 5 mm 重建。pseudo-LR 与真实 LR 的域差是这个领域的已知问题。
2. **"可检出率"是对比度 > 100 HU 的代理判据**，非医师实际检出；真实检出待阅片研究。
3. **我们的方法在病灶检出上不优于骨干**（LUNA16 94.9% vs 94.9%）；贡献在定量与可审查性。
4. **形态保真度不如确定性骨干**（IoU ≥ 0.3 时 precision 低 5–8 个点）。转诊工作流下影响有限，但需声明。
5. **逐例证书只保证全局密度**，不保证局部。分块版本已证伪：数据一致性抓不住"层厚方向平均正确"的幻觉（覆盖率与随机无异）。
6. **采样离散度的定位能力 n=8**（扩样中）。
7. **CCC 受受试者间方差影响**：本项目实测误差最小的分层 CCC 最低（0.519）、误差最大的最高（0.532）。故 CCC 必须与 MAE、bias 同报。
8. **协议常数不可跨设备迁移**：实测 τ_δ 跨协议差 17 倍，故证书需现场标定或协议知识库。

---

# 附录 B：阅片研究方案（完整版）

目标：**重建的 1 mm 相比原始 5 mm，是否让医生更容易判断病灶？以及重建是否会造出不存在的病灶？**

第二问和第一问同等重要。乡镇医院场景下没有薄层可对照，一个凭空造出的结节会变成一次不必要的
转诊或穿刺，而医生没有办法自己发现它是假的。

---

## 1. 必须是三臂，不能是两臂

| 臂 | 作用 |
|---|---|
| A. 原始 5 mm | 下界 = 现状 |
| B. **重建 1 mm** | 待评 |
| C. **真实 1 mm** | 上界，同时是**判定 B 中所见是真是假的唯一依据** |

**只有 A、B 两臂时，这个研究回答不了核心问题。** 医生在 B 中报告一个结节，你无法区分
"5 mm 漏了"和"重建造的"——而这两种情况的临床含义完全相反。C 臂是判据，不是锦上添花。

同一批患者、三臂配对，随机化呈现顺序，医生对臂别盲法。

---

## 2. 病例构成

| 类别 | 建议例数 | 作用 |
|---|---|---|
| 含结节，**直径 < 6 mm** | ≥ 40 | 主要靶点：公开数据实测厚层在此档漏 31% |
| 含结节，6–10 mm | ≥ 30 | 中间档 |
| 含结节，≥ 10 mm | ≥ 20 | 厚层已能检出，作阴性对照（重建不应更差） |
| **确证无结节** | ≥ 40 | **测假阳性的唯一途径** |

无结节组不能省。没有它，"重建造不造假病灶"这个问题在统计上无法回答——所有阳性发现都会被
归因于"5 mm 漏了"。

**结节真值**：以 C 臂（真实 1 mm）为准，由不参与阅片的两名放射科医师独立标注、分歧共识解决，
并报告标注者间一致性（CLAIM 第 17 条）。

---

## 3. 医生任务与记录项

每例每臂记录：

1. **是否发现结节**（是/否）
2. 若是：**位置**（层号 + 肺叶）、**最大径**、**性质**（实性/部分实性/磨玻璃）
3. **置信度**（1–5 分李克特）
4. **图像质量主观评分**（1–5：噪声、边缘锐利度、伪影）
5. 若怀疑图像有**人工痕迹**，单独标记（这一项直接对应幻觉）

同一名医生阅同一患者的不同臂之间**间隔 ≥ 2 周**（洗脱期），避免记忆效应。

至少 **3 名**放射科医师，报告医师间一致性。

---

## 4. 主要与次要终点

**主要终点**：< 6 mm 结节的**检出灵敏度**，B 臂 vs A 臂（配对）。

**次要终点**：

- 无结节病例的**假阳性率**，B 臂 vs A 臂 —— 安全性主终点
- B 臂 vs C 臂的灵敏度差（重建离真薄层还有多远）
- 置信度与主观图像质量评分
- **JAFROC / FROC** 分析（同时利用位置与置信度，比单纯灵敏度更充分）
- 阅片时间

---

## 5. 与自动指标的对照

阅片结果出来后，必须回头核对自动指标是否预测了它。这是让自动指标可信的唯一途径：

- **采样离散度**（`--samples 6 --save_std`）标出的可疑区域，是否与医生标记的"人工痕迹"重合？公开数据上排序富集 AUC = 0.919，需在真人上验证
- **逐例证书**判为 flag/abstain 的病例，医生评分是否更差？
- 自动"对比度保留"是否与医生检出相关？

若离散度在真人研究中同样有效，它就能作为**部署时的自动提示**——这是确定性方法原理上给不出的东西。

---

## 6. 素材导出（上机时一并做，回来补不了）

```bash
python scripts/export_reader_study.py --root PRIVATE/henan/pairs \
    --recon RECON/henan --out READER --n 130
```

导出内容：

- 三臂 NIfTI，文件名**已随机化并打乱臂别**（对照表留在院内，不出院）
- `reader_manifest.csv`：随机 ID → 臂别 → 患者 sha1（**不出院**）
- `reader_blank.csv`：医生填写用的空表

**注意**：阅片素材是图像，**不能带出医院**。阅片须在院内完成，或按院方数据管理规定另行审批。
带出来的只有填好的评分表（不含图像、不含患者标识）。
