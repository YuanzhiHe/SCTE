# 交接：5 mm 基线收尾（三件）

## 一、删掉重复的脚本

仓库里原有一份 `scripts/thick_baseline.py`（commit ab0e7ca，2026-09-18），与你们新写的那份
功能重叠。**已在本端删除，保留你们的版本。**

两份的实质差别只有两处，你们的都更合理：

| | 仓库旧版（已删） | 你们的版本（保留） |
|---|---|---|
| 逐肺叶小肺叶守卫 | > 1000 体素 | > 200 体素 |
| PSNR / SSIM | 未计算 | 厚层最近邻复制到 1 mm 网格后计算 |

第一行解释了 `5df24add2311` 为何只在 strict 变体被剔除：用 >1000 的守卫，它在两个变体
中都会被剔。第二行的约定有实际含义——最近邻复制正是把 5 mm 铺在 1 mm 网格上阅片时
看到的东西。

那边 `git pull` 后如果工作区里还有你们自己的 `scripts/thick_baseline.py`，直接 `git add`
提交即可；若本地文件恰好也叫这个名字，pull 会因删除与新增冲突而停下，`git checkout
--ours scripts/thick_baseline.py` 保留本地版本再提交。

---

## 二、改一句机制表述

**不要写**："连最简单的线性插值重建都比不重建强。"

Lanczos 在四个端点上均显著优于直测，这是事实；但机制不是"恢复信息"——上采样不可能
增加信息。真实原因是 Lanczos 核带负瓣，插值时会过冲振铃，制造出局部范围之外的低密度
值，偶然抵消了一部分容积平均。

**建议表述**：

> 直接在 5 mm 序列上测量的偏倚大于 Lanczos 插值后测量（四个端点的配对差异 95% 置信
> 区间均不含 0）。这并非插值恢复了信息，而是其核的负瓣所产生的过冲在低密度侧部分
> 抵消了轴向部分容积平均；该效应不可控，也不随层厚改善。

写成"恢复了信息"会被审稿人直接抓住。

---

## 三、两个样本量要在正文分开交代

现在报告里同时存在 n = 444 与 n = 438，必须说明来源，否则会被问。

- **n = 444**：各臂各自完整跑完的病例数，用于表 1 至表 3 的描述性统计（偏差、一致性界、CCC）。
- **n = 438**：所有臂的共同子集，用于配对 bootstrap 检验。剔除 6 例，原因是既定政策——
  任一端点在任一臂出现非有限值即整例剔除。

**建议在表 1 下方加一句**：

> 表中各臂的统计量在其各自完整的 444 例上计算；后文的配对比较在所有臂的共同子集
> （438 例）上进行，6 例因某一臂出现非有限值而按既定政策整例剔除。共同子集的参考
> 分布（LAA-950 7.82 ± 8.14%）与全队列（7.82 ± 8.21%）一致。

剔除的 6 例及原因（供正文或补充材料列出）：

| 病例 | 原因 |
|---|---|
| 014a91cc8d28、09b38e258336 | SCTE-R（站点适配）重建含非有限值 |
| a5ad259cf3f2、af66b248c554 | SCTE-R（零样本）重建含非有限值 |
| acfa623c667e | 全部臂，右上叶过小 |
| 5df24add2311 | 仅 strict 变体，右下叶厚网格体素数低于守卫 |

前四例的非有限值是模型发散所致（肺掩膜正常），已在 `scte_r/agentic.py` 中改为判为
abstain（commit 19cdb66）；这一点应写进局限性。
