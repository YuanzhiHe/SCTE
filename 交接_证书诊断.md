# 交接：证书诊断三步（不占 GPU）

面向执行方。三步都是纯 CPU 的读表操作，**不要碰正在跑的 stage 9**（PID 21932）。
每步都给了"要回传什么"，请照格式贴回，不要只贴结论。

工作目录：`E:\medical_data_backup\SCTE`（下文用相对路径）

---

## 步骤 0：拉代码

```bash
git pull
```

需要的两个脚本：`scripts/why_flagged.py`、`scripts/common_subset.py`。

---

## 步骤 1：找回 pooled 的真阈值，重跑 pooled 诊断

**背景**：上一份诊断里 pooled 那一块的 `tau_delta=2.00 / tau_rho=0.150 / tau_s=2.00`
正好是脚本内置的默认值，说明 `--tau` 指向的文件没找到或缺键，走了 fallback。
证据是 pooled-oracle 里 structural 单独否决 293 例、实际 flagged 只有 53 例——
293 > 53 不可能，判决是用真阈值算的，否决数是用默认值重算的。所以那三行的
**否决数全部作废**（判决数 378/56 仍有效）。

### 1a. 定位真文件

```bash
grep -rl tau_delta PRIVATE/hebei --include=*.json
```

`run_private.sh:213-220` 会把 τ 写回 `$PROTO`，正确的那份里应该同时有
`protocol_w`、`tau_delta`、`tau_rho`、`tau_s`。

### 1b. 用它重跑

把 `<真protocol.json>` 换成上一步找到的路径：

```bash
python scripts/why_flagged.py \
  --cert PRIVATE/hebei/results/cert_oracle.csv:pooled-oracle \
         PRIVATE/hebei/results/cert_flow_zeroshot.csv:pooled-flow \
  --tau <真protocol.json>
```

### 回传

**必须包含输出的第一行**（`阈值来源 ...`）。如果它写的是 `default`，说明文件还是
没找到，先解决这个再往下，不要贴结果。

---

## 步骤 2：区分"零分布重尾"和"标定集迁移不过去"

**背景**：证书要三项同时通过。三个独立的 2-sd 检验，即使零分布完美也有
1−0.975³ ≈ 7% 的名义拒绝率。g0-oracle 实测拒绝 24%（每项约 10%），是名义值的
4 倍。两种可能的病因要吃两种药，必须先分清。

两个 CSV 已经存在，不用重算：
- `cert_oracle.csv` = **标定集**上的 oracle（阈值就是从它拟的）
- `cert_oracle_test.csv` = **测试集**上的 oracle

```bash
for g in g0 g1; do
  python scripts/why_flagged.py \
    --cert PRIVATE/hebei/by_scanner/$g/cert_oracle.csv:$g-oracle-CAL \
           PRIVATE/hebei/by_scanner/$g/cert_oracle_test.csv:$g-oracle-TEST \
    --tau PRIVATE/hebei/by_scanner/$g/protocol.json
done
```

Windows PowerShell 写法：

```powershell
foreach ($g in "g0","g1") {
  python scripts/why_flagged.py `
    --cert "PRIVATE/hebei/by_scanner/$g/cert_oracle.csv:$g-oracle-CAL" `
           "PRIVATE/hebei/by_scanner/$g/cert_oracle_test.csv:$g-oracle-TEST" `
    --tau "PRIVATE/hebei/by_scanner/$g/protocol.json"
}
```

### 怎么读（只回传数据，判断这边做）

- **CAL 和 TEST 的中位数/90分位相近** → 零分布重尾，`|均值|+2sd` 的高斯假设不成立
- **CAL 明显更窄** → 标定集迁移不过去。`EXT_CAL` 是按排序取前 50 例的，
  排序若和机器/日期/院区相关，两边就不可交换

### 回传

两组各两块的完整输出。

### 附带查一件事

g1 有 12/127 (9.4%) 例 abstain，g0 只有 1/317 (0.3%)，相差 30 倍。
abstain 的原因是 `empty_lung_interior`：

```bash
python -c "
import csv,collections
for g in ('g0','g1'):
    for f in ('cert_oracle_test.csv','cert_flow.csv'):
        p='PRIVATE/hebei/by_scanner/%s/%s'%(g,f)
        try: rows=list(csv.DictReader(open(p)))
        except OSError: continue
        c=collections.Counter(r['verdict'] for r in rows)
        print(g,f,dict(c))"
```

---

## 步骤 3：补齐第 1 节那张表的空格

**背景**：主结果表里 baseline 的 SSIM 和 ≤5pp 是空的，而且三条臂应当在**同一批
病例**上比。三个 CSV 都是 444 行，交集就是全集，但脚本会把这一点确认下来。

```bash
python scripts/common_subset.py \
  --arms PRIVATE/hebei/results/volume_lanczos.csv:Lanczos \
         PRIVATE/hebei/results/volume_zs_cthnet.csv:CTHNet \
         PRIVATE/hebei/results/volume_zs_ours.csv:Ours \
  --csv PRIVATE/hebei/results/common_subset.csv
```

输出里有每条臂的 bias、MAE、95% LoA、CCC，以及对 Ours 的配对 |误差| 差和自助法
95% 置信区间。**bias + 95% LoA 是临床期刊的标准写法，要作为主指标**；≤2pp 那个
留作次要终点。

### 回传

完整输出，尤其是 `laa950` 和 `p15` 两块，以及顶部的"共同完成的病例 N 例"。

---

## 三步都不要做的事

- 不要改阈值定义。步骤 2 的结果决定改成分位数还是改标定集抽样方式，两种改法不一样。
- 不要做那个 δ̂ 标量改正。这个项目早先记录过：事后按证书改正，各项指标全部变差；
  而且 δ̂ 是证书自己的统计量，拟标量去压它等于让证书失效。真要做，判据必须是
  **对 1mm 参考的 LAA/Perc 一致性有没有改善**，不能是 δ̂ 变小了。
- 不要动 stage 9。

---

## stage 9 跑完之后（需要 GPU，约 1 小时 + 2–3 小时）

### 4a. pooled 对照组

```bash
python scripts/per_scanner.py \
  --cal PRIVATE/hebei/pairs_external/cal \
  --test PRIVATE/hebei/pairs_external/test \
  --out PRIVATE/hebei/by_scanner \
  --pooled_control
```

g0/g1 两组一步都不会重做（脚本对 `protocol.json`、`forward_op.pt`、`cert_*.csv`
都有存在性检查），只补 pooled 一组。它是唯一能把"阈值换了队列"（内部 TRAIN 203 例
→ 外部标定 32/18 例）和"分了机器"这两件事拆开的对照。

### 4b. 混合臂的整卷指标

目前 hybrid 只有证书、没有 PSNR/SSIM/LAA。而 hybrid 是唯一可能同时拿到 CTHNet 的
图像质量和 Ours 的密度学的臂：

```bash
python scripts/infer_volume.py \
  --root PRIVATE/hebei/pairs_external/test \
  --ckpt public_flow.pt --flow --base_recon \
  --residual_scale 0.028 --flow_steps 32 --n 444 \
  --csv PRIVATE/hebei/results/volume_zs_hybrid.csv
```

CSV 是增量写的，`--start <已完成数>` 可续跑。跑完把它加进步骤 3 的 `--arms` 里。

---

## 一句话背景（执行方可以不看）

目前定位到的绑定项是 **structural (rho)**：oracle 否决 0–10.7%，模型臂否决
80–88%，两台机器一致。displacement 反而是过关的（δ̂ 均值 −0.7 到 −1.5 HU，
否决率 0–2.8%）。也就是说位移已经被控制住了，证书是按另一个理由拒绝的——
这与"LAA950 有 64% 落在 2pp 内"是自洽的。
